---
phase: 05-reliability-and-performance-consistency
plan: 2
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/chat/components/UnifiedInboxPanel.vue
  - src/features/chat/components/GlobalSearch.vue
  - src/features/chat/stores/retrievalStore.ts
  - tests/components/UnifiedInboxPanel.performance.test.ts
  - tests/components/GlobalSearch.performance.test.ts
autonomous: true
requirements:
  - RELI-02
must_haves:
  truths:
    - User can scroll and process a large inbox without rendering every row eagerly.
    - User can search across conversations and page through results without noticeable navigation delay.
    - Search-result jumps keep the existing best-effort preload behavior but stop waiting past a short latency budget.
  artifacts:
    - path: src/features/chat/components/UnifiedInboxPanel.vue
      provides: Virtualized inbox rendering for long lists
    - path: src/features/chat/components/GlobalSearch.vue
      provides: Virtualized/bounded search result rendering and timed preload navigation
    - path: src/features/chat/stores/retrievalStore.ts
      provides: Stable result-session state for large paginated result sets
    - path: tests/components/GlobalSearch.performance.test.ts
      provides: Regression coverage for render budget and bounded jump latency
  key_links:
    - from: src/features/chat/components/UnifiedInboxPanel.vue
      to: @tanstack/vue-virtual
      via: useVirtualizer over inbox items
      pattern: useVirtualizer
    - from: src/features/chat/components/GlobalSearch.vue
      to: src/features/chat/stores/retrievalStore.ts
      via: retrievalStore.results / canLoadMore / loadMore
      pattern: retrievalStore\.(results|canLoadMore|loadMore)
    - from: src/features/chat/components/GlobalSearch.vue
      to: loadInboxEventContext
      via: Promise.race timeout budget before router.push
      pattern: Promise\.race
---

<objective>
Reduce inbox/search interaction cost under heavier result volumes so Phase 5 improves responsiveness without changing product surface area.

Purpose: Deliver RELI-02 by reusing the repo's existing virtualization and non-blocking navigation patterns instead of introducing new search flows or routes.
Output: Performance-focused component tests plus virtualized/bounded inbox and GlobalSearch rendering.
</objective>

<execution_context>
@/Users/shikun/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/shikun/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-reliability-and-performance-consistency/05-RESEARCH.md
@.planning/phases/05-reliability-and-performance-consistency/05-VALIDATION.md
@src/features/chat/components/UnifiedInboxPanel.vue
@src/features/chat/components/GlobalSearch.vue
@src/features/chat/stores/retrievalStore.ts
@src/features/chat/components/ConversationList.vue
@tests/components/UnifiedInboxPanel.test.ts
@tests/components/GlobalSearch.test.ts
@.planning/phases/03-cross-conversation-retrieval/03-02-SUMMARY.md
@.planning/phases/01-unified-inbox-triage/01-03-SUMMARY.md

<interfaces>
From `src/features/chat/stores/retrievalStore.ts`:
```ts
export const useRetrievalStore = defineStore('retrieval', () => {
  const results = ref<RetrievalItem[]>([])
  const canLoadMore = ref(false)
  async function search(nextQuery: string)
  async function loadMore()
})
```

From `src/features/chat/components/ConversationList.vue`:

```ts
const virtualizer = useVirtualizer(computed(() => ({
  count: conversations.value.length,
  getScrollElement: () => scrollRef.value,
  estimateSize: () => ITEM_HEIGHT,
  overscan: 6,
})))
```

From `src/features/chat/components/GlobalSearch.vue`:

```ts
async function jumpToResult(roomId: string, eventId: string) {
  await loadInboxEventContext(roomId, eventId)
  await router.push({ path: `/chat/${encodeURIComponent(roomId)}`, query: { focusEventId: eventId } })
}
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock inbox/search responsiveness expectations with failing component tests</name>
  <files>tests/components/UnifiedInboxPanel.performance.test.ts, tests/components/GlobalSearch.performance.test.ts</files>
  <behavior>
    - Test 1: Large inbox datasets render only the visible slice / virtual rows instead of every item node at once.
    - Test 2: GlobalSearch large result sets remain bounded and paginated, while joined-room filtering still hides left-room hits.
    - Test 3: Jump-to-result preload is best-effort with a short timeout budget, so navigation proceeds even when preload stalls.
  </behavior>
  <action>Add RED component tests using large fixture arrays and fake timers. Assert DOM row counts stay bounded, load-more still works, and navigation falls through after the timeout budget rather than hanging on preload. Do not change production code in this task, and do not introduce incremental search-on-type requirements that are not in the roadmap.</action>
  <verify>
    <automated>! pnpm vitest run tests/components/UnifiedInboxPanel.performance.test.ts tests/components/GlobalSearch.performance.test.ts -x</automated>
  </verify>
  <done>The new performance tests exist and fail because current inbox/search rendering is still eager and preload has no bounded wait.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Virtualize inbox and GlobalSearch rendering while keeping jump navigation non-blocking</name>
  <files>src/features/chat/components/UnifiedInboxPanel.vue, src/features/chat/components/GlobalSearch.vue, src/features/chat/stores/retrievalStore.ts, tests/components/UnifiedInboxPanel.performance.test.ts, tests/components/GlobalSearch.performance.test.ts</files>
  <behavior>
    - Test 1: Inbox list stays responsive for large item counts by rendering through a virtual window.
    - Test 2: GlobalSearch keeps result rendering bounded while preserving joined-room filtering, pagination, and current query/result lifecycle.
    - Test 3: Result jumps preserve the existing preload-first behavior but continue routing after a short timeout instead of waiting indefinitely.
  </behavior>
  <action>Reuse the existing `@tanstack/vue-virtual` pattern from `ConversationList.vue` for inbox and message-hit rendering. Keep room quick results capped and message result state stable through root replacement in `retrievalStore`; do not replace the Pinia store with a new architecture, and do not change the existing submit-driven search UX. Update `GlobalSearch.vue` jump handling to use a bounded timeout (for example ~250ms) around `loadInboxEventContext`, then preserve the existing warning + direct navigation fallback. Do not add new routes, background workers, or server-side indexing work in this phase.</action>
  <verify>
    <automated>pnpm vitest run tests/components/UnifiedInboxPanel.performance.test.ts tests/components/GlobalSearch.performance.test.ts tests/components/UnifiedInboxPanel.test.ts tests/components/GlobalSearch.test.ts -x</automated>
  </verify>
  <done>Inbox/search heavy-result flows are virtualized or otherwise bounded, load-more still works, joined-room filtering still protects visibility, and navigation remains responsive under stalled preload.</done>
</task>

</tasks>

<verification>
- `pnpm vitest run tests/components/UnifiedInboxPanel.performance.test.ts tests/components/GlobalSearch.performance.test.ts tests/components/UnifiedInboxPanel.test.ts tests/components/GlobalSearch.test.ts -x`
- Verify the implementation reuses `@tanstack/vue-virtual` instead of a hand-rolled windowing system.
</verification>

<success_criteria>

- Unified inbox no longer eagerly renders every row for large datasets.
- GlobalSearch keeps pagination, scope filtering, and jump behavior while staying responsive.
- Bounded preload latency protects navigation without removing the existing fallback warning path.
  </success_criteria>

<output>
After completion, create `.planning/phases/05-reliability-and-performance-consistency/05-02-SUMMARY.md`
</output>
