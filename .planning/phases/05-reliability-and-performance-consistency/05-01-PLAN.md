---
phase: 05-reliability-and-performance-consistency
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - src/matrix/types.ts
  - src/matrix/events.ts
  - src/matrix/sync.ts
  - src/matrix/rooms.ts
  - src/features/chat/composables/useUnifiedInbox.ts
  - tests/mocks/recovery.ts
  - tests/unit/matrix/syncRecovery.test.ts
  - tests/components/UnifiedInboxPanel.recovery.test.ts
autonomous: true
requirements:
  - RELI-01
must_haves:
  truths:
    - User sees the same inbox-derived work items after reconnect or sync catch-up without waiting for a brand-new message.
    - Limited timeline / recovery refreshes do not regress inbox ordering or silently drop the latest visible event context.
  artifacts:
    - path: src/matrix/sync.ts
      provides: Full sync lifecycle handling plus recovery event emission
    - path: src/matrix/rooms.ts
      provides: Canonical room-summary derivation from live timeline APIs
    - path: src/features/chat/composables/useUnifiedInbox.ts
      provides: Recovery-triggered inbox recomputation
    - path: tests/unit/matrix/syncRecovery.test.ts
      provides: Regression coverage for reconnect/catch-up/gap handling
  key_links:
    - from: src/matrix/sync.ts
      to: src/matrix/events.ts
      via: matrixEvents.emit('sync.state', { state })
      pattern: matrixEvents\.emit\('sync\.state'
    - from: src/features/chat/composables/useUnifiedInbox.ts
      to: src/matrix/rooms.ts
      via: getRoomSummaries() during recovery refresh
      pattern: getRoomSummaries\(
---

<objective>
Harden reconnect and sync-gap recovery so inbox-derived work items stay consistent after Matrix catch-up.

Purpose: Deliver the Matrix-facing half of RELI-01 without introducing a new parallel state system; reuse Matrix room state as the canonical recovery source for inbox refresh.
Output: Recovery regression tests plus sync/event/room/inbox implementation updates that survive reconnect and limited-timeline scenarios.
</objective>

<execution_context>
@/Users/shikun/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/shikun/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-reliability-and-performance-consistency/05-RESEARCH.md
@.planning/phases/05-reliability-and-performance-consistency/05-VALIDATION.md
@src/matrix/types.ts
@src/matrix/events.ts
@src/matrix/sync.ts
@src/matrix/rooms.ts
@src/features/chat/composables/useUnifiedInbox.ts
@tests/components/UnifiedInboxPanel.test.ts

<interfaces>
From `src/matrix/types.ts`:
```ts
export type SyncState = 'PREPARED' | 'SYNCING' | 'ERROR' | 'STOPPED'

export interface RoomSummary {
  roomId: string
  lastMessage?: string
  lastMessageTs?: number
  unreadCount: number
  highlightCount: number
}
```

From `src/matrix/events.ts`:
```ts
type MatrixEvents = {
  'room.message': { roomId: string, event: any }
  'room.member': { roomId: string, userId: string, membership: string }
  'room.receipt': { roomId: string, eventId: string, userId: string }
  'sync.state': { state: string }
}
```

From `src/features/chat/composables/useUnifiedInbox.ts`:
```ts
export function useUnifiedInbox() {
  return { isLoading, items, allItems, counts, refresh: refreshNow }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock reconnect and catch-up inbox recovery behavior with failing tests</name>
  <files>tests/mocks/recovery.ts, tests/unit/matrix/syncRecovery.test.ts, tests/components/UnifiedInboxPanel.recovery.test.ts</files>
  <behavior>
    - Test 1: Matrix sync transitions through RECONNECTING/CATCHUP/PREPARED/SYNCING and emits recovery-visible state changes instead of silently ignoring them.
    - Test 2: Unified inbox recomputes from canonical room summaries after recovery and does not wait for a fresh `room.message` event before showing the restored item set.
    - Test 3: Limited-timeline recovery keeps latest visible inbox ordering stable instead of dropping back to stale summary data.
  </behavior>
  <action>Create shared recovery mock builders for sync state sequences, joined-room summaries, and limited-timeline fixtures. Add RED tests only — do not patch production logic yet. The failing assertions must specifically expose the current gaps: missing `sync.state` emission from the sync bridge, missing recovery lifecycle states, and deprecated/stale summary assumptions after reconnect.</action>
  <verify>
    <automated>! pnpm vitest run tests/unit/matrix/syncRecovery.test.ts tests/components/UnifiedInboxPanel.recovery.test.ts -x</automated>
  </verify>
  <done>The targeted recovery test files exist, share stable fixtures, and fail for the intended current reconnect gaps.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement canonical sync and inbox reconciliation for reconnect recovery</name>
  <files>src/matrix/types.ts, src/matrix/events.ts, src/matrix/sync.ts, src/matrix/rooms.ts, src/features/chat/composables/useUnifiedInbox.ts, tests/mocks/recovery.ts, tests/unit/matrix/syncRecovery.test.ts, tests/components/UnifiedInboxPanel.recovery.test.ts</files>
  <behavior>
    - Test 1: Recovery lifecycle states are modeled explicitly and emitted to listeners.
    - Test 2: Inbox refresh after reconnect/catch-up re-derives from current Matrix room data and preserves stable ordering/item ids.
    - Test 3: Limited-timeline recovery keeps the latest visible event summary instead of regressing to stale timeline access.
  </behavior>
  <action>Extend `SyncState` to include the official reconnect/catch-up states needed by the repo. Emit `matrixEvents.emit('sync.state', { state })` from the sync bridge for each observed state, while keeping existing error retry behavior intact. Update room summary derivation to use `room.getLiveTimeline().getEvents()` rather than deprecated `room.timeline`, and keep latest visible-event lookup best-effort instead of assuming a perfect timeline. Make `useUnifiedInbox()` treat recovery events as canonical refresh triggers. Do not introduce a second recovery-specific inbox store, and do not block navigation or wait on manual recovery confirmation.</action>
  <verify>
    <automated>pnpm vitest run tests/unit/matrix/syncRecovery.test.ts tests/components/UnifiedInboxPanel.recovery.test.ts tests/unit/matrix/inbox.test.ts -x</automated>
  </verify>
  <done>Reconnect/catch-up transitions produce deterministic inbox refreshes from canonical room data, and the targeted recovery regressions pass.</done>
</task>

</tasks>

<verification>
- `pnpm vitest run tests/unit/matrix/syncRecovery.test.ts tests/components/UnifiedInboxPanel.recovery.test.ts tests/unit/matrix/inbox.test.ts -x`
- Confirm no new code path replaces the existing preload-then-fallback navigation rule.
</verification>

<success_criteria>
- Unified inbox refreshes from canonical Matrix room data on reconnect/catch-up.
- Recovery regressions are locked by automated tests named in 05-VALIDATION.md.
</success_criteria>

<output>
After completion, create `.planning/phases/05-reliability-and-performance-consistency/05-01-SUMMARY.md`
</output>
