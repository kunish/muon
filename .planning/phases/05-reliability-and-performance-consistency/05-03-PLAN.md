---
phase: 05-reliability-and-performance-consistency
plan: 3
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/chat/stores/taskStore.ts
  - tests/unit/stores/taskStore.recovery.test.ts
autonomous: true
requirements:
  - RELI-01
must_haves:
  truths:
    - User-created tasks remain available after reconnect/bootstrap instead of being silently cleared or duplicated.
    - Task persistence tolerates repeated hydrate/recovery entry with invalid legacy rows filtered out deterministically.
  artifacts:
    - path: src/features/chat/stores/taskStore.ts
      provides: Idempotent task continuity across hydrate/recovery bootstrap
    - path: tests/unit/stores/taskStore.recovery.test.ts
      provides: Regression coverage for persisted task continuity
  key_links:
    - from: src/features/chat/stores/taskStore.ts
      to: localStorage
      via: hydrate and persistState
      pattern: localStorage\.(getItem|setItem)
---

<objective>
Preserve persisted task continuity through reconnect/bootstrap so RELI-01 covers both inbox recovery and task execution state.

Purpose: Keep Phase 2's local task model trustworthy under recovery pressure without introducing server-synced task state.
Output: RED→GREEN task continuity tests and a tightly scoped `taskStore` hydrate/persistence hardening change.
</objective>

<execution_context>
@/Users/shikun/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/shikun/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-reliability-and-performance-consistency/05-RESEARCH.md
@.planning/phases/05-reliability-and-performance-consistency/05-VALIDATION.md
@src/features/chat/stores/taskStore.ts
@tests/unit/stores/taskStore.test.ts
@.planning/phases/02-defer-and-message-to-task-loop/02-03-SUMMARY.md

<interfaces>
From `src/features/chat/stores/taskStore.ts`:
```ts
export const useTaskStore = defineStore('task', () => {
  const tasks = ref<TaskItem[]>([])
  const hydrated = ref(false)
  function hydrate()
  function createTask(input: CreateTaskInput): TaskItem
  function transitionStatus(id: string, to: TaskStatus)
})
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing task recovery tests for repeated hydrate/bootstrap entry</name>
  <files>tests/unit/stores/taskStore.recovery.test.ts</files>
  <behavior>
    - Test 1: repeated hydrate/recovery entry preserves valid persisted tasks exactly once.
    - Test 2: invalid legacy rows are filtered while valid rows remain available after recovery bootstrap.
    - Test 3: local task updates remain writable after the recovery-oriented hydrate path runs.
  </behavior>
  <action>Create RED tests targeted only at task continuity. Use existing localStorage-backed patterns from `taskStore.test.ts`; do not add network mocks or expand scope into defer state unless the task tests prove it is required.</action>
  <verify>
    <automated>! pnpm vitest run tests/unit/stores/taskStore.recovery.test.ts -x</automated>
  </verify>
  <done>The recovery-specific taskStore tests exist and fail because current hydrate/bootstrap behavior is not explicitly guarded for reconnect re-entry.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Harden taskStore hydrate for reconnect-safe continuity</name>
  <files>src/features/chat/stores/taskStore.ts, tests/unit/stores/taskStore.recovery.test.ts</files>
  <behavior>
    - Test 1: hydrate/recovery re-entry is idempotent.
    - Test 2: persisted valid tasks survive recovery while malformed rows are ignored.
    - Test 3: follow-up task creation/status transitions still work after recovery bootstrap.
  </behavior>
  <action>Adjust `taskStore` so hydrate semantics remain safe when the store is revisited during reconnect/bootstrap. Preserve the existing Phase 2 decision that task state lives in the Pinia store + versioned localStorage key, and keep status transitions centralized in store actions. Do not add remote task synchronization or a new persistence layer.</action>
  <verify>
    <automated>pnpm vitest run tests/unit/stores/taskStore.recovery.test.ts tests/unit/stores/taskStore.test.ts -x</automated>
  </verify>
  <done>Task continuity is explicitly guaranteed by tests and store logic, with no silent task loss across reconnect/bootstrap entry.</done>
</task>

</tasks>

<verification>
- `pnpm vitest run tests/unit/stores/taskStore.recovery.test.ts tests/unit/stores/taskStore.test.ts -x`
- Confirm `TASK_STORAGE_KEY` remains the persistence source of truth and no new persistence layer is introduced.
</verification>

<success_criteria>
- Persisted tasks survive reconnect/bootstrap re-entry without duplication or silent loss.
- Existing task creation and status transitions still pass after the continuity hardening change.
</success_criteria>

<output>
After completion, create `.planning/phases/05-reliability-and-performance-consistency/05-03-SUMMARY.md`
</output>
