---
phase: 01-unified-inbox-triage
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/matrix/inbox.ts
  - src/matrix/index.ts
  - tests/unit/matrix/inbox.test.ts
  - tests/mocks/matrix.ts
autonomous: true
requirements: [INBX-03]
must_haves:
  truths:
    - 用户触发跳转时，系统能拿到目标消息及其前后文
    - 本地 live timeline 缺少目标消息时，仍可加载上下文
  artifacts:
    - path: src/matrix/inbox.ts
      provides: Inbox source-context loader API
    - path: tests/unit/matrix/inbox.test.ts
      provides: INBX-03 context loading behavior tests
  key_links:
    - from: src/matrix/inbox.ts
      to: matrix-js-sdk client
      via: getEventTimeline + paginateEventTimeline
      pattern: "getEventTimeline|paginateEventTimeline"
    - from: src/matrix/index.ts
      to: src/matrix/inbox.ts
      via: re-export
      pattern: "from './inbox'"
---

<objective>
实现 INBX-03 的上下文加载能力封装：给任意 inbox item 提供“目标消息 + 前后文”数据。

Purpose: 把最易出错的 Matrix 上下文拉取单独封装并测试，避免 UI 层手写协议细节。
Output: `loadInboxEventContext` API、导出入口、单测与 mock 扩展。
</objective>

<execution_context>
@/Users/shikun/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/shikun/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-unified-inbox-triage/01-CONTEXT.md
@.planning/phases/01-unified-inbox-triage/01-RESEARCH.md
@src/matrix/index.ts
@src/matrix/messages.ts
@tests/mocks/matrix.ts

<interfaces>
From src/matrix/index.ts:
```ts
export { getTimeline, paginateBack } from './messages'
```

Target contract to create in this plan:
```ts
export interface InboxEventContext {
  roomId: string
  eventId: string
  eventsBefore: MatrixEvent[]
  event: MatrixEvent
  eventsAfter: MatrixEvent[]
}
export async function loadInboxEventContext(roomId: string, eventId: string, limit?: number): Promise<InboxEventContext>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 写上下文加载契约与失败测试（接口先行）</name>
  <files>src/matrix/inbox.ts, tests/unit/matrix/inbox.test.ts, tests/mocks/matrix.ts</files>
  <action>先在 `src/matrix/inbox.ts` 写 `InboxEventContext` 与 `loadInboxEventContext` 方法签名（可先抛错）；再创建单测覆盖：命中目标事件时返回 event+before+after、本地 timeline 缺失目标时走回填路径、失败时抛出可识别错误。扩展 mock 支持 `getEventTimeline/paginateEventTimeline`。</action>
  <verify>
    <automated>pnpm test:unit -- tests/unit/matrix/inbox.test.ts</automated>
  </verify>
  <done>测试文件与契约文件存在，且测试用例明确描述 INBX-03 行为预期。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现 Matrix context loader 并从 matrix/index 导出</name>
  <files>src/matrix/inbox.ts, src/matrix/index.ts, tests/unit/matrix/inbox.test.ts</files>
  <behavior>
    - Test 1: `loadInboxEventContext` 返回目标 event 与前后文数组
    - Test 2: 当 event 不在当前窗口时，仍可通过 SDK timeline API 拉到上下文
    - Test 3: 失败路径返回可处理异常（非静默失败）
  </behavior>
  <action>优先实现 SDK 路径：`getEventTimeline` + 前后向 `paginateEventTimeline`；如无法获取目标 event，按 research 结论加入 `/context` 回填分支。最终通过 `src/matrix/index.ts` re-export 给 UI 层使用；禁止在组件内直接拼 Matrix context 请求。</action>
  <verify>
    <automated>pnpm test:unit -- tests/unit/matrix/inbox.test.ts</automated>
  </verify>
  <done>INBX-03 所需上下文数据可通过统一 API 获得，且单测通过。</done>
</task>

</tasks>

<verification>
执行 `pnpm test:unit -- tests/unit/matrix/inbox.test.ts` 通过。
</verification>

<success_criteria>
- 存在可复用的 `loadInboxEventContext`，而非散落在 UI 的临时实现。
- 目标消息缺失于 live timeline 时仍可回填上下文。
</success_criteria>

<output>
After completion, create `.planning/phases/01-unified-inbox-triage/01-02-SUMMARY.md`
</output>
