---
phase: 01-unified-inbox-triage
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/chat/types/unifiedInbox.ts
  - src/features/chat/stores/inboxStore.ts
  - src/features/chat/composables/useUnifiedInbox.ts
  - tests/unit/stores/inboxStore.test.ts
  - tests/mocks/matrix.ts
autonomous: true
requirements: [INBX-01, INBX-02]
must_haves:
  truths:
    - 用户可以在统一收件箱数据源中看到 mentions、priority unread、reply-needed 三类事项
    - 用户可以按类型过滤并批量标记为 processed
    - processed 状态刷新后仍保留（仅本地持久化）
  artifacts:
    - path: src/features/chat/types/unifiedInbox.ts
      provides: Unified inbox contracts
    - path: src/features/chat/stores/inboxStore.ts
      provides: Selection and batch processed actions
    - path: src/features/chat/composables/useUnifiedInbox.ts
      provides: Aggregation/filter/sort derived state
  key_links:
    - from: src/features/chat/composables/useUnifiedInbox.ts
      to: src/matrix/rooms.ts
      via: getRoomSummaries + matrixEvents
      pattern: "highlightCount|unreadCount"
    - from: src/features/chat/stores/inboxStore.ts
      to: localStorage
      via: processed state serialization
      pattern: "muon:inbox:processed:v1"
---

<objective>
实现 Unified Inbox 的基础数据层（契约 + store + composable），严格满足已锁定决策：priority unread 基于 Matrix highlight/mention，processed 仅本地持久化。

Purpose: 先把 INBX-01/02 的可测试核心能力做成稳定接口，供后续 UI 与跳转链路直接消费。
Output: unifiedInbox 类型契约、inboxStore、useUnifiedInbox、对应单测。
</objective>

<execution_context>
@/Users/shikun/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/shikun/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-unified-inbox-triage/01-CONTEXT.md
@.planning/phases/01-unified-inbox-triage/01-RESEARCH.md
@src/features/chat/stores/chatStore.ts
@src/features/chat/composables/useConversations.ts
@src/matrix/rooms.ts

<interfaces>
From src/matrix/types.ts:
```ts
export interface RoomSummary {
  roomId: string
  name: string
  unreadCount: number
  highlightCount: number
  isDirect: boolean
  lastMessageTs?: number
}
```

From src/features/chat/stores/chatStore.ts:

```ts
const selectedMessages = reactive(new Set<string>())
function toggleMessageSelection(eventId: string): void
```

From src/features/chat/composables/useConversations.ts:

```ts
for (const evt of LISTENED_EVENTS)
  matrixEvents.on(evt, scheduleRefresh)
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 定义 Unified Inbox 接口契约（接口先行）</name>
  <files>src/features/chat/types/unifiedInbox.ts</files>
  <action>创建统一收件箱契约：`UnifiedInboxItemType`（`mention | priority-unread | reply-needed`）、`UnifiedInboxItem`、`InboxFilterType`、`InboxProcessedState`、`INBOX_PROCESSED_STORAGE_KEY = "muon:inbox:processed:v1"`。此任务只定义类型与常量，不写聚合逻辑。</action>
  <verify>
    <automated>pnpm type-check</automated>
  </verify>
  <done>下游任务可以直接按统一类型实现，无需二次探索字段定义。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 以 TDD 实现 inboxStore 与 useUnifiedInbox 核心逻辑</name>
  <files>src/features/chat/stores/inboxStore.ts, src/features/chat/composables/useUnifiedInbox.ts, tests/unit/stores/inboxStore.test.ts, tests/mocks/matrix.ts</files>
  <behavior>
    - Test 1: 聚合结果必须包含 mentions / priority-unread / reply-needed 三类 item（INBX-01）
    - Test 2: priority-unread 判定以 highlight/mention 为核心（遵循锁定决策）
    - Test 3: 过滤器仅返回对应类型 item，且支持 all（INBX-02）
    - Test 4: 批量 processed 后刷新可恢复（localStorage），且不做跨设备同步（遵循锁定决策）
  </behavior>
  <action>先写并运行失败测试，再实现最小通过代码。reply-needed 采用可单测的最小规则：`latest visible event sender != currentUser && unreadCount > 0`（不引入 NLP）；批量选择与 processed 写入全部经 store action，禁止组件直接改 Set。若研究建议与用户锁定决策冲突，仍按锁定决策实现并在代码注释注明原因。</action>
  <verify>
    <automated>pnpm test:unit -- tests/unit/stores/inboxStore.test.ts</automated>
  </verify>
  <done>INBX-01/02 对应单测通过，且 processed 明确为本地持久化实现。</done>
</task>

</tasks>

<verification>
执行 `pnpm test:unit -- tests/unit/stores/inboxStore.test.ts` 与 `pnpm type-check` 均通过。
</verification>

<success_criteria>

- INBX-01 的三类聚合结果可由自动化测试证明。
- INBX-02 的过滤与批量 processed 可由自动化测试证明。
- processed 只保存在本地存储，不包含任何跨设备同步逻辑。
  </success_criteria>

<output>
After completion, create `.planning/phases/01-unified-inbox-triage/01-01-SUMMARY.md`
</output>
