---
phase: 01-unified-inbox-triage
plan: "03"
type: execute
wave: 2
depends_on: [01-01, 01-02]
files_modified:
  - src/features/chat/components/UnifiedInboxPanel.vue
  - src/features/server/components/ChannelSidebar.vue
  - src/features/chat/components/MessageList.vue
  - src/locales/en.json
  - src/locales/zh.json
  - tests/components/UnifiedInboxPanel.test.ts
autonomous: true
requirements: [INBX-01, INBX-02, INBX-03]
must_haves:
  truths:
    - 用户可在一个入口看到 unified inbox 并按类型筛选
    - 用户可批量把选中事项标记为 processed
    - 用户点击任一事项后可跳到源消息并看到上下文
  artifacts:
    - path: src/features/chat/components/UnifiedInboxPanel.vue
      provides: Unified inbox UI and interactions
    - path: src/features/server/components/ChannelSidebar.vue
      provides: Inbox entry in DM mode
    - path: src/features/chat/components/MessageList.vue
      provides: Focus/scroll behavior for target event
  key_links:
    - from: src/features/chat/components/UnifiedInboxPanel.vue
      to: src/features/chat/stores/inboxStore.ts
      via: filter/select/process actions
      pattern: "setFilter|toggleSelection|markSelectedProcessed"
    - from: src/features/server/components/ChannelSidebar.vue
      to: src/matrix/inbox.ts
      via: loadInboxEventContext before navigation
      pattern: "loadInboxEventContext"
    - from: src/features/server/components/ChannelSidebar.vue
      to: src/features/chat/components/MessageList.vue
      via: route query focusEventId
      pattern: "focusEventId"
---

<objective>
交付 Phase 1 的最终用户能力：在统一收件箱中完成筛选/批处理，并从事项一键跳回源消息上下文。

Purpose: 把前两个计划的能力接入真实 DM 入口与消息视图，形成可直接验收的 INBX-01/02/03 闭环。
Output: UnifiedInboxPanel、Sidebar 集成、消息落位行为、组件测试。
</objective>

<execution_context>
@/Users/shikun/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/shikun/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-unified-inbox-triage/01-CONTEXT.md
@.planning/phases/01-unified-inbox-triage/01-RESEARCH.md
@.planning/phases/01-unified-inbox-triage/01-01-SUMMARY.md
@.planning/phases/01-unified-inbox-triage/01-02-SUMMARY.md
@src/features/server/components/ChannelSidebar.vue
@src/features/chat/components/MessageList.vue

<interfaces>
From Plan 01 contracts:
```ts
type UnifiedInboxItemType = 'mention' | 'priority-unread' | 'reply-needed'
interface UnifiedInboxItem { roomId: string; eventId: string; type: UnifiedInboxItemType; createdTs: number }
```

From Plan 02 matrix API:

```ts
loadInboxEventContext(roomId: string, eventId: string, limit?: number): Promise<InboxEventContext>
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 实现 UnifiedInboxPanel 组件与交互测试</name>
  <files>src/features/chat/components/UnifiedInboxPanel.vue, tests/components/UnifiedInboxPanel.test.ts, src/locales/en.json, src/locales/zh.json</files>
  <behavior>
    - Test 1: 面板展示三类 item，并支持 all/mention/priority-unread/reply-needed 过滤
    - Test 2: 多选后触发批量 processed，UI 状态即时更新
    - Test 3: 点击条目触发 jump 事件并携带 roomId/eventId
  </behavior>
  <action>实现 UnifiedInboxPanel（列表、过滤标签、多选与批处理按钮、空态）；文案补充到 `en/zh` locale。严格保持简洁交互，不加入 deferred 的高级动画与复杂可视化。</action>
  <verify>
    <automated>pnpm test:unit -- tests/components/UnifiedInboxPanel.test.ts</automated>
  </verify>
  <done>INBX-01/02 的用户可见交互在组件层成立，测试通过。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 在 DM 侧边栏接入收件箱并打通源消息跳转</name>
  <files>src/features/server/components/ChannelSidebar.vue, src/features/chat/components/MessageList.vue, tests/components/UnifiedInboxPanel.test.ts</files>
  <behavior>
    - Test 1: DM 模式可见 UnifiedInboxPanel
    - Test 2: 点击 inbox item 时先加载 context，再导航到对应 `/dm/:roomId` 并携带 focusEventId
    - Test 3: MessageList 读取 focusEventId 后滚动定位到目标 event（非仅跳房间）
  </behavior>
  <action>在 `ChannelSidebar.vue` 的 DM 模式中挂载 `UnifiedInboxPanel`；处理 `jump` 时调用 `loadInboxEventContext` 后再导航（INBX-03）。在 `MessageList.vue` 增加对 `focusEventId` 的定位逻辑，定位成功后清理 query，避免重复跳转。</action>
  <verify>
    <automated>pnpm test:unit -- tests/components/UnifiedInboxPanel.test.ts && pnpm test:unit -- tests/unit/matrix/inbox.test.ts</automated>
  </verify>
  <done>INBX-03 从 UI 到消息列表形成闭环，且自动化测试通过。</done>
</task>

</tasks>

<verification>
执行：
1. `pnpm test:unit -- tests/unit/stores/inboxStore.test.ts`
2. `pnpm test:unit -- tests/unit/matrix/inbox.test.ts`
3. `pnpm test:unit -- tests/components/UnifiedInboxPanel.test.ts`
</verification>

<success_criteria>

- 用户可在统一入口看到并处理三类事项（INBX-01/02）。
- 用户可从任一事项跳到源消息并看到上下文（INBX-03）。
- 不包含任何 deferred 范围（跨设备同步、智能优先级、高级动画）。
  </success_criteria>

<output>
After completion, create `.planning/phases/01-unified-inbox-triage/01-03-SUMMARY.md`
</output>
