---
phase: 07-offline-digest-reliability-and-knowledge-continuity
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/unit/stores/digestStore.test.ts
  - tests/unit/stores/decisionStore.test.ts
  - tests/components/OfflineDigestPanel.test.ts
  - tests/components/DecisionPanel.test.ts
autonomous: true
requirements:
  - DIGE-01
  - DIGE-02
  - DIGE-03
  - DECI-02
must_haves:
  truths:
    - "Automated coverage proves initializeDigest does not overwrite hydrated entries when sourceEvents is empty."
    - "Automated coverage proves buildDigestSession merges with (not replaces) hydrated entries on empty materialization."
    - "Automated coverage proves decisionStore.hydrateCards only materializes digest-backed suggestions from the latest session."
    - "Automated coverage proves OfflineDigestPanel remount shows previously persisted entries."
  artifacts:
    - path: "tests/unit/stores/digestStore.test.ts"
      provides: "Digest reopen/merge/empty-session guard coverage"
      contains: "preserves hydrated entries"
    - path: "tests/unit/stores/decisionStore.test.ts"
      provides: "Session-scoped digest suggestion materialization coverage"
      contains: "latest session"
    - path: "tests/components/OfflineDigestPanel.test.ts"
      provides: "Panel remount entry preservation coverage"
      contains: "remount"
    - path: "tests/components/DecisionPanel.test.ts"
      provides: "Digest-backed suggestion visibility coverage"
      contains: "digest-backed"
  key_links:
    - from: "tests/unit/stores/digestStore.test.ts"
      to: "src/features/chat/stores/digestStore.ts"
      via: "initializeDigest + buildDigestSession lifecycle"
      pattern: "initializeDigest|buildDigestSession"
    - from: "tests/unit/stores/decisionStore.test.ts"
      to: "src/features/chat/stores/decisionStore.ts"
      via: "hydrateCards with session-scoped digest entries"
      pattern: "hydrateCards|sessionId"
    - from: "tests/components/OfflineDigestPanel.test.ts"
      to: "src/features/chat/components/OfflineDigestPanel.vue"
      via: "mount/unmount/remount lifecycle"
      pattern: "initializeDigest|entries"
---

<objective>
Lock Phase 7's digest reliability and knowledge continuity contract in failing tests before implementing fixes.

Purpose: Prove the three identified gaps — empty-session overwrite, stale session suggestion leakage, and panel remount entry loss — exist in the current codebase by writing tests that describe the correct behavior and go RED against the current implementation.
Output: Extended digestStore, decisionStore, OfflineDigestPanel, and DecisionPanel test suites with new test cases that capture the reliability gaps.
</objective>

<execution_context>
@/Users/shikun/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/shikun/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-offline-digest-reliability-and-knowledge-continuity/07-RESEARCH.md
@.planning/phases/07-offline-digest-reliability-and-knowledge-continuity/07-VALIDATION.md
@.planning/phases/04-offline-digest-and-decision-capture/04-02-SUMMARY.md
@.planning/phases/04-offline-digest-and-decision-capture/04-06-SUMMARY.md
@.planning/phases/04-offline-digest-and-decision-capture/04-07-SUMMARY.md
@src/features/chat/stores/digestStore.ts
@src/features/chat/stores/decisionStore.ts
@src/features/chat/components/OfflineDigestPanel.vue
@src/features/chat/components/DecisionPanel.vue
@src/features/chat/types/digest.ts
@src/features/chat/types/knowledge.ts
@src/shared/lib/knowledgeDb.ts
@tests/unit/stores/digestStore.test.ts
@tests/unit/stores/decisionStore.test.ts
@tests/components/OfflineDigestPanel.test.ts
@tests/components/DecisionPanel.test.ts

<interfaces>
From `src/features/chat/stores/digestStore.ts`:
```ts
async function initializeDigest(options: { now?: number, lastOfflineAt?: number | null } = {})
async function hydrateDigestEntries()
async function buildDigestSession(options: { now?: number, lastOfflineAt?: number | null } = {})
function ingestEvent(event: DigestSourceEvent)
function startRuntimeSync()
function stopRuntimeSync()
const entries = ref<DigestEntry[]>([])
const sourceEvents = ref<DigestSourceEvent[]>([])
```

From `src/features/chat/stores/decisionStore.ts`:
```ts
async function hydrateCards()
async function materializeSuggestionsFromDigest(entry: DigestEntry)
const cards = ref<DecisionCard[]>([])
```

From `src/shared/lib/knowledgeDb.ts`:
```ts
async listDigestEntries(relevance?: DigestEntry['relevance']): Promise<DigestEntry[]>
async listDecisionCards(status?: DecisionCard['status']): Promise<DecisionCard[]>
async saveDigestEntry(entry: DigestEntry): Promise<DigestEntry>
async saveDecisionCard(card: DecisionCard): Promise<DecisionCard>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add digestStore empty-session overwrite and merge RED tests</name>
  <files>tests/unit/stores/digestStore.test.ts</files>
  <action>Extend the existing digestStore test suite with two new tests:

1. **"initializeDigest preserves hydrated entries when sourceEvents is empty and lastOfflineAt is set"** — Mock `listDigestEntries` to return 2 saved entries. Call `initializeDigest({ lastOfflineAt: 1000, now: 2000 })` with an empty `sourceEvents` ref. Assert that `entries.value` still contains the 2 hydrated entries after the call completes, not an empty array.

2. **"buildDigestSession does not overwrite hydrated entries with empty materialization"** — Directly set `entries.value` to 2 entries (simulating prior hydration). Call `buildDigestSession({ lastOfflineAt: 1000, now: 2000 })` with empty `sourceEvents`. Assert that `entries.value` still has the 2 entries, not `[]`.

Use the existing mock patterns from the file (dynamic import, mocked knowledgeDb, mocked matrix modules). Both tests should go RED because the current `buildDigestSession` unconditionally sets `entries.value = nextSession.entries` which is `[]` when sourceEvents is empty.</action>
  <verify>
    <automated>pnpm vitest run tests/unit/stores/digestStore.test.ts</automated>
  </verify>
  <done>Two new digestStore tests go RED, confirming the empty-session overwrite gap exists.</done>
</task>

<task type="auto">
  <name>Task 2: Add decisionStore session-scoped suggestion materialization RED tests</name>
  <files>tests/unit/stores/decisionStore.test.ts</files>
  <action>Extend the existing decisionStore test suite with two new tests:

1. **"hydrateCards only materializes digest-backed suggestions from the latest session"** — Mock `listDigestEntries` to return 3 entries: 2 with `sessionId: 'session-current'` and 1 with `sessionId: 'session-old'`. The old-session entry should have action/blocker keywords. Call `hydrateCards()`. Assert that only the 2 current-session entries produced suggestion cards, not the old-session entry.

2. **"stale digest entries from older sessions do not generate new suggestion cards"** — Mock `listDigestEntries` to return only old-session entries with action/blocker keywords. Call `hydrateCards()`. Assert that `cards.value` has zero digest-backed cards (only manually-created cards from `listDecisionCards` if any).

Use the existing mock patterns. Both tests should go RED because the current `hydrateCards` reads ALL digest entries without session filtering.</action>
  <verify>
    <automated>pnpm vitest run tests/unit/stores/decisionStore.test.ts</automated>
  </verify>
  <done>Two new decisionStore tests go RED, confirming the stale-session suggestion leakage gap exists.</done>
</task>

<task type="auto">
  <name>Task 3: Add OfflineDigestPanel remount and DecisionPanel digest-backed visibility RED tests</name>
  <files>tests/components/OfflineDigestPanel.test.ts, tests/components/DecisionPanel.test.ts</files>
  <action>Extend the existing component test suites:

1. **OfflineDigestPanel — "remounting panel after unmount shows previously persisted entries"** — Mount the panel, populate `digestStore.entries` with 2 entries, unmount the panel, then remount. Mock `listDigestEntries` to return the same 2 entries (simulating Dexie persistence). Assert that the remounted panel renders 2 digest entry articles. This should go RED because `initializeDigest` on remount calls `buildDigestSession` which overwrites with empty materialization.

2. **DecisionPanel — "digest-backed suggestions are visible when latest-session digest entries exist"** — Mock `listDigestEntries` to return entries with action/blocker keywords from `sessionId: 'latest'` and some from `sessionId: 'old'`. Mock `listDecisionCards` to return empty. Call mount. Assert that only latest-session suggestions appear as decision cards. This should go RED because current code materializes all sessions.

Use existing mock patterns from each test file.</action>
  <verify>
    <automated>pnpm vitest run tests/components/OfflineDigestPanel.test.ts tests/components/DecisionPanel.test.ts</automated>
  </verify>
  <done>OfflineDigestPanel remount test and DecisionPanel session-scoped suggestion test go RED, confirming the panel-level reliability gaps exist.</done>
</task>

</tasks>

<verification>
Run `pnpm vitest run tests/unit/stores/digestStore.test.ts tests/unit/stores/decisionStore.test.ts tests/components/OfflineDigestPanel.test.ts tests/components/DecisionPanel.test.ts` before handing off. The new tests must go RED (fail) while existing tests remain GREEN (pass).
</verification>

<success_criteria>
- All 6 new tests go RED for the correct architectural reasons identified in 07-RESEARCH.md.
- All existing tests in the 4 files remain GREEN (no regression).
- The RED failures describe the exact gaps: empty-session overwrite, stale-session suggestion leakage, and panel remount entry loss.
</success_criteria>

<output>
After completion, create `.planning/phases/07-offline-digest-reliability-and-knowledge-continuity/07-01-SUMMARY.md`
</output>
