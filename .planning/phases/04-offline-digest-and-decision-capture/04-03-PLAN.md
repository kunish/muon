---
phase: 04-offline-digest-and-decision-capture
plan: "03"
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/features/chat/types/decision.ts
  - src/features/chat/stores/decisionStore.ts
  - src/features/chat/components/DecisionPanel.vue
  - tests/unit/stores/decisionStore.test.ts
  - tests/components/DecisionPanel.test.ts
autonomous: true
requirements: [DECI-01, DECI-02]
must_haves:
  truths:
    - "用户可创建包含结论、背景、owner、状态、关联消息的决策卡。"
    - "AI 建议项默认 pending，用户可以明确 accept/reject。"
    - "建议项处置状态可追溯（包含时间戳与操作者信息）。"
  artifacts:
    - path: "src/features/chat/stores/decisionStore.ts"
      provides: "Decision card CRUD and suggestion disposition transitions"
      contains: "setSuggestionDisposition"
    - path: "src/features/chat/components/DecisionPanel.vue"
      provides: "Decision card UI and accept/reject actions"
      contains: "accepted"
    - path: "tests/unit/stores/decisionStore.test.ts"
      provides: "Store behavior coverage for DECI-01/DECI-02"
      contains: "suggestion disposition"
  key_links:
    - from: "src/features/chat/components/DecisionPanel.vue"
      to: "src/features/chat/stores/decisionStore.ts"
      via: "card create/update and suggestion disposition actions"
      pattern: "useDecisionStore"
    - from: "src/features/chat/stores/decisionStore.ts"
      to: "src/shared/lib/knowledgeDb.ts"
      via: "decision/suggestion persistence"
      pattern: "decisions"
    - from: "src/features/chat/stores/decisionStore.ts"
      to: "src/features/chat/types/knowledge.ts"
      via: "citation-required payload contracts"
      pattern: "citations"
---

<objective>
实现决策沉淀与 AI 建议处置闭环：用户可创建标准化决策卡，并对建议行动项/阻塞项执行 accept/reject。

Purpose: 让“讨论结论”从瞬时聊天变为可审计知识资产，覆盖 DECI-01/DECI-02。
Output: decision 类型、decisionStore、DecisionPanel 与完整测试护栏。
</objective>

<execution_context>
@/Users/shikun/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/shikun/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-offline-digest-and-decision-capture/04-RESEARCH.md
@.planning/phases/04-offline-digest-and-decision-capture/04-01-SUMMARY.md
@src/features/chat/stores/taskStore.ts
@src/features/chat/components/TaskPanel.vue

<interfaces>
From src/features/chat/stores/taskStore.ts (transition discipline to mirror):
```typescript
function transitionStatus(id: string, to: TaskStatus)
```

From 04-01 output:
```typescript
export type SuggestionDisposition = 'pending' | 'accepted' | 'rejected'
export interface CitationRef { roomId: string; eventId: string; quote?: string }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 先写 decision store + panel RED 测试（Wave 0）</name>
  <files>tests/unit/stores/decisionStore.test.ts, tests/components/DecisionPanel.test.ts</files>
  <behavior>
    - Test 1: createDecisionCard 必须保存 conclusion/context/owner/status/citations。
    - Test 2: AI 建议 disposition 默认 pending，仅可转为 accepted 或 rejected。
    - Test 3: accept/reject 后保留审计字段（updatedAt、updatedBy）。
  </behavior>
  <action>新增 `decisionStore` 与 `DecisionPanel` 的失败测试，先固定 DECI-01/02 的输入输出契约；对非法状态转移、空 citations、缺失 owner 写明确断言。保持 RED，不要先补实现。</action>
  <verify>
    <automated>pnpm vitest run tests/unit/stores/decisionStore.test.ts tests/components/DecisionPanel.test.ts -x</automated>
  </verify>
  <done>DECI-01/02 行为边界被测试锁定，失败原因与未实现逻辑一致。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现 decisionStore 与 DecisionPanel（GREEN）</name>
  <files>src/features/chat/types/decision.ts, src/features/chat/stores/decisionStore.ts, src/features/chat/components/DecisionPanel.vue, tests/unit/stores/decisionStore.test.ts, tests/components/DecisionPanel.test.ts</files>
  <behavior>
    - Test 1: decisionStore 提供 card CRUD 与状态更新动作，状态迁移集中在 store action。
    - Test 2: suggestion disposition 转移持久化并记录审计字段。
    - Test 3: DecisionPanel 可创建决策卡并在 UI 上执行 accept/reject。
  </behavior>
  <action>实现 `decision.ts` 类型与 `decisionStore.ts` 业务动作：决策卡创建/更新、建议处置（pending→accepted/rejected）和审计时间戳持久化；`DecisionPanel.vue` 渲染表单、卡片列表、建议项 accept/reject 控件并调用 store。必须保持 citation 必填；禁止组件直接改写持久化状态（一律经 store actions）。</action>
  <verify>
    <automated>pnpm vitest run tests/unit/stores/decisionStore.test.ts tests/components/DecisionPanel.test.ts -x</automated>
  </verify>
  <done>用户可完整创建决策卡并对 AI 建议做可追溯 accept/reject，DECI-01/02 测试全绿。</done>
</task>

</tasks>

<verification>
运行：`pnpm vitest run tests/unit/stores/decisionStore.test.ts tests/components/DecisionPanel.test.ts`。
</verification>

<success_criteria>
- DECI-01：决策卡包含 conclusion/context/owner/status/linked messages 并可持久化。
- DECI-02：AI 建议支持 accept/reject 且处置状态可审计。
</success_criteria>

<output>
After completion, create `.planning/phases/04-offline-digest-and-decision-capture/04-03-SUMMARY.md`
</output>
