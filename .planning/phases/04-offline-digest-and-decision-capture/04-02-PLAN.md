---
phase: 04-offline-digest-and-decision-capture
plan: "02"
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/features/chat/types/digest.ts
  - src/matrix/digest.ts
  - src/features/chat/stores/digestStore.ts
  - src/features/chat/components/OfflineDigestPanel.vue
  - tests/unit/stores/digestStore.test.ts
  - tests/components/OfflineDigestPanel.test.ts
autonomous: true
requirements: [DIGE-01, DIGE-02, DIGE-03]
must_haves:
  truths:
    - "用户回归后能看到离线窗口内的精简 digest 列表。"
    - "digest 条目可按 responsibility/follow/mention 相关性排序。"
    - "每条 digest 都可点击 citation 回跳源消息并定位。"
  artifacts:
    - path: "src/features/chat/stores/digestStore.ts"
      provides: "Away-window digest session + relevance sort state"
      contains: "buildDigestSession"
    - path: "src/features/chat/components/OfflineDigestPanel.vue"
      provides: "Digest UI with citation jump and relevance filter"
      contains: "openCitation"
    - path: "src/matrix/digest.ts"
      provides: "Matrix event ingestion/materialization for offline digest"
      contains: "materializeOfflineDigest"
    - path: "tests/unit/stores/digestStore.test.ts"
      provides: "Digest logic coverage for away-window and relevance"
      contains: "relevance"
  key_links:
    - from: "src/features/chat/components/OfflineDigestPanel.vue"
      to: "src/matrix/inbox.ts"
      via: "citation preload and focusEventId navigation"
      pattern: "loadInboxEventContext"
    - from: "src/features/chat/stores/digestStore.ts"
      to: "src/matrix/digest.ts"
      via: "event materialization pipeline"
      pattern: "materializeOfflineDigest"
    - from: "src/features/chat/stores/digestStore.ts"
      to: "src/shared/lib/knowledgeDb.ts"
      via: "digest snapshot persistence"
      pattern: "digestEntries"
    - from: "src/features/chat/stores/digestStore.ts"
      to: "src/matrix/rooms.ts"
      via: "temporary follow mapping from pinned/favourite rooms"
      pattern: "isPinned"
---

<objective>
实现离线 digest 主链路：生成 away-window 摘要、按个人相关性排序、并支持 citation 验证回跳。

Purpose: 先兑现用户“回归即可快速抓重点并验证依据”的核心收益，覆盖 DIGE-01/02/03。
Output: digest materializer、digest store、OfflineDigestPanel 与对应单测/组件测试。
</objective>

<execution_context>
@/Users/shikun/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/shikun/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-offline-digest-and-decision-capture/04-RESEARCH.md
@.planning/phases/04-offline-digest-and-decision-capture/04-01-SUMMARY.md
@src/features/chat/components/GlobalSearch.vue
@src/shared/composables/useNetworkStatus.ts
@src/matrix/events.ts
@src/matrix/rooms.ts

<interfaces>
From 04-01 output (consume directly):
```typescript
export interface CitationRef {
  roomId: string
  eventId: string
  quote?: string
}
```

From src/shared/composables/useNetworkStatus.ts:

```typescript
export function useNetworkStatus(): { status: Ref<ConnectionStatus>, lastOfflineAt: Ref<number | null> }
```

From src/matrix/events.ts:

```typescript
export const matrixEvents: Emitter<{
  'room.message': { roomId: string, event: any }
  'sync.state': { state: string }
}>
```

From src/matrix/rooms.ts:

```typescript
export function getRoomSummaries(): RoomSummary[]
// RoomSummary includes isPinned/highlightCount for per-room relevance signals
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 先写 digest store + panel 的 RED 测试（Wave 0）</name>
  <files>tests/unit/stores/digestStore.test.ts, tests/components/OfflineDigestPanel.test.ts</files>
  <behavior>
    - Test 1: lastOfflineAt 到当前时间窗口内的事件会被聚合为 digest session。
    - Test 2: relevance 排序严格为 responsibility > follow > mention（同级按时间倒序）。
    - Test 3: `follow` 临时映射为 pinned/favourite rooms（`RoomSummary.isPinned === true`），并在测试中写明该迁移约束。
    - Test 4: 点击 citation 时先尝试 loadInboxEventContext，再带 focusEventId 导航；预加载失败仅 warning。
  </behavior>
  <action>创建 `digestStore` 和 `OfflineDigestPanel` 的测试文件，先固定 DIGE-01/02/03 行为边界；mock matrixEvents、network status、router 与 `loadInboxEventContext`。**在本任务先锁定 `follow` 的临时数据来源：使用 pinned/favourite rooms（`RoomSummary.isPinned === true`）作为 follow 信号，并在测试命名/断言中写明 “future explicit follow model may replace this mapping”。** 保持 RED：不要提前实现业务模块。</action>
  <verify>
    <automated>pnpm vitest run tests/unit/stores/digestStore.test.ts tests/components/OfflineDigestPanel.test.ts -x</automated>
  </verify>
  <done>digest 需求行为已测试化且当前失败，失败点仅来自待实现逻辑。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现 digest materialization/store/panel 并转绿</name>
  <files>src/features/chat/types/digest.ts, src/matrix/digest.ts, src/features/chat/stores/digestStore.ts, src/features/chat/components/OfflineDigestPanel.vue, tests/unit/stores/digestStore.test.ts, tests/components/OfflineDigestPanel.test.ts</files>
  <behavior>
    - Test 1: digestStore 能生成 away-window 摘要并持久化快照。
    - Test 2: relevance bucket 可切换，默认按 responsibility/follow/mention 排序，其中 `follow` 明确来自 pinned/favourite rooms。
    - Test 3: OfflineDigestPanel 展示摘要+引用并复用 preload+fallback 导航链路。
  </behavior>
  <action>实现 `src/matrix/digest.ts` 的事件归并函数（输入 room.message 流与 offline window，输出 citation-first digest entries）；在 `digestStore.ts` 接入 `knowledgeDb` 存储与 relevance 排序；**把 `follow` 明确实现为 pinned/favourite room 映射（优先读取 `RoomSummary.isPinned` / room tag），并将该映射结果持久化为 digest entry 的 relevance reason，方便后续迁移到显式 follow 模型。** 在 `OfflineDigestPanel.vue` 渲染 digest 条目、相关性筛选、citation 列表和点击回跳。禁止新建平行导航链路，必须复用 `loadInboxEventContext + focusEventId`；禁止把 digest 简化为未读列表直出。</action>
  <verify>
    <automated>pnpm vitest run tests/unit/stores/digestStore.test.ts tests/components/OfflineDigestPanel.test.ts -x</automated>
  </verify>
  <done>DIGE-01/02/03 对应测试通过，用户可完成“回归看摘要→按相关性聚焦→引用验证”的闭环。</done>
</task>

</tasks>

<verification>
运行：`pnpm vitest run tests/unit/stores/digestStore.test.ts tests/components/OfflineDigestPanel.test.ts`。
</verification>

<success_criteria>

- DIGE-01：离线窗口 digest 可生成并展示。
- DIGE-02：每条摘要具备可点击 citation 且回跳到源消息。
- DIGE-03：摘要支持按 responsibility/follow/mention 稳定排序。
  </success_criteria>

<output>
After completion, create `.planning/phases/04-offline-digest-and-decision-capture/04-02-SUMMARY.md`
</output>
