---
phase: 04-offline-digest-and-decision-capture
plan: "08"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/chat/stores/qaStore.ts
  - src/features/chat/services/crossSessionQa.ts
  - src/features/chat/components/CrossSessionQaPanel.vue
  - tests/unit/stores/qaStore.test.ts
  - tests/unit/services/crossSessionQa.test.ts
  - tests/components/CrossSessionQaPanel.test.ts
autonomous: true
requirements: [DECI-03]
gap_closure: true
must_haves:
  truths:
    - "用户重新打开 qa tab 时，可以读回已保存问答，而不是只看到空态。"
    - "用户仍可提跨会话问题并得到带 citations 的回答。"
    - "QA citation 继续复用 joined-room 结果与 preload + focusEventId 回跳链路。"
  artifacts:
    - path: "src/features/chat/stores/qaStore.ts"
      provides: "QA history hydration and active-answer state"
      contains: "hydrateHistory"
    - path: "src/features/chat/services/crossSessionQa.ts"
      provides: "QA ask + history listing service surface"
      contains: "listSavedQaSessions"
    - path: "src/features/chat/components/CrossSessionQaPanel.vue"
      provides: "QA panel that restores saved answers on mount"
      contains: "onMounted"
    - path: "tests/unit/stores/qaStore.test.ts"
      provides: "QA history restore coverage"
      contains: "hydrates saved qa sessions"
  key_links:
    - from: "src/features/chat/stores/qaStore.ts"
      to: "src/features/chat/services/crossSessionQa.ts"
      via: "hydrate + ask orchestration"
      pattern: "listSavedQaSessions|askCrossSessionQuestion"
    - from: "src/features/chat/services/crossSessionQa.ts"
      to: "src/shared/lib/knowledgeDb.ts"
      via: "saveQaSession + listQaSessions"
      pattern: "saveQaSession|listQaSessions"
    - from: "src/features/chat/components/CrossSessionQaPanel.vue"
      to: "src/matrix/inbox.ts"
      via: "citation preload and focus navigation"
      pattern: "loadInboxEventContext"
---

<objective>
补齐 QA 历史恢复链路：Knowledge 的 QA 子视图重开后能读回已保存问答，同时保留现有跨会话 cited-answer 行为。

Purpose: 关闭 verification 报告中“QA 只保存不回读”的剩余缺口，确保 DECI-03 在面板重开场景下仍完整成立。
Output: qaStore、支持 history list 的 crossSessionQa service、可恢复历史的 CrossSessionQaPanel，以及对应测试。
</objective>

<execution_context>
@/Users/shikun/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/shikun/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-offline-digest-and-decision-capture/04-RESEARCH.md
@.planning/phases/04-offline-digest-and-decision-capture/04-VERIFICATION.md
@.planning/phases/04-offline-digest-and-decision-capture/04-04-SUMMARY.md
@src/shared/lib/knowledgeDb.ts
@src/features/chat/services/crossSessionQa.ts
@src/features/chat/components/CrossSessionQaPanel.vue

<interfaces>
From src/features/chat/services/crossSessionQa.ts:
```typescript
export async function askCrossSessionQuestion(question: string, limit = 5): Promise<CrossSessionQaAnswer>
```

From src/shared/lib/knowledgeDb.ts:
```typescript
createKnowledgeRepository().saveQaSession(answer)
createKnowledgeRepository().listQaSessions()
```

From src/features/chat/types/knowledge.ts:
```typescript
export type CrossSessionQaAnswer = {
  id: string
  question: string
  answer: string
  citations: CitationRef[]
  createdAt: number
  updatedAt: number
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 先锁定 QA 历史恢复与面板回显的失败测试</name>
  <files>tests/unit/stores/qaStore.test.ts, tests/unit/services/crossSessionQa.test.ts, tests/components/CrossSessionQaPanel.test.ts</files>
  <behavior>
    - Test 1: `qaStore.hydrateHistory()` 会从持久化层恢复问答历史，并把最新答案设为默认显示项。
    - Test 2: `crossSessionQa` 除提问外还暴露 `listSavedQaSessions()`，按时间倒序返回持久化结果。
    - Test 3: `CrossSessionQaPanel` 挂载时显示最近一次已保存回答；再次提问后会更新当前答案但不丢历史。
    - Test 4: citation 点击仍复用 preload + fallback navigation。
  </behavior>
  <action>新增 `qaStore` 单测，并扩展 service/component 测试覆盖历史回读、默认展示最近答案、再次提问后的历史保留。mock `listQaSessions`、`askCrossSessionQuestion`、router 与 `loadInboxEventContext`。保持 RED：当前实现应失败在缺少 store、缺少 history list API 或 panel 未回显历史上。</action>
  <verify>
    <automated>pnpm vitest run tests/unit/stores/qaStore.test.ts tests/unit/services/crossSessionQa.test.ts tests/components/CrossSessionQaPanel.test.ts</automated>
  </verify>
  <done>QA 历史恢复行为被测试化，且当前失败点准确指向 store/service/panel 缺口。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现 qaStore、历史读取 API 与 QA 面板回显</name>
  <files>src/features/chat/stores/qaStore.ts, src/features/chat/services/crossSessionQa.ts, src/features/chat/components/CrossSessionQaPanel.vue, tests/unit/stores/qaStore.test.ts, tests/unit/services/crossSessionQa.test.ts, tests/components/CrossSessionQaPanel.test.ts</files>
  <behavior>
    - Test 1: QA 面板挂载时展示最近一次已保存问答。
    - Test 2: submit 问题后会持久化并把新答案置顶，同时保留旧历史。
    - Test 3: citation jump 仍然 preload-first、失败降级导航。
  </behavior>
  <action>创建 `src/features/chat/stores/qaStore.ts`，提供 `history`、`activeAnswer`、`hydrateHistory()`、`askQuestion()` 等 API；在 `crossSessionQa.ts` 中补充 `listSavedQaSessions()`，继续复用现有 joined-room 检索与 `saveQaSession()` 持久化。更新 `CrossSessionQaPanel.vue`：挂载时调用 `qaStore.hydrateHistory()`，当存在历史时默认渲染最新答案；提交新问题走 `qaStore.askQuestion()`，并在 UI 中保留历史可见性（至少保留最近问题列表或最近答案回显）。禁止新建平行导航链路，citation 点击仍必须复用 `loadInboxEventContext + focusEventId`。</action>
  <verify>
    <automated>pnpm vitest run tests/unit/stores/qaStore.test.ts tests/unit/services/crossSessionQa.test.ts tests/components/CrossSessionQaPanel.test.ts</automated>
  </verify>
  <done>DECI-03 在“重开知识面板”场景下闭环成立：历史问答可恢复，新问答继续带 citations 持久化与回跳。</done>
</task>

</tasks>

<verification>
运行：`pnpm vitest run tests/unit/stores/qaStore.test.ts tests/unit/services/crossSessionQa.test.ts tests/components/CrossSessionQaPanel.test.ts`。
</verification>

<success_criteria>
- DECI-03：QA 子视图重开后可恢复已保存答案，并继续生成带 citations 的新答案。
- QA 历史恢复不改变 joined-room 检索边界，也不改变 citation jump 行为。
</success_criteria>

<output>
After completion, create `.planning/phases/04-offline-digest-and-decision-capture/04-08-SUMMARY.md`
</output>
