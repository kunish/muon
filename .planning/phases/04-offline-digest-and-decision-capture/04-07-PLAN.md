---
phase: 04-offline-digest-and-decision-capture
plan: "07"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/chat/services/suggestionExtraction.ts
  - src/features/chat/stores/decisionStore.ts
  - src/features/chat/components/DecisionPanel.vue
  - tests/unit/services/suggestionExtraction.test.ts
  - tests/unit/stores/decisionStore.test.ts
  - tests/components/DecisionPanel.test.ts
autonomous: true
requirements: [DECI-01, DECI-02]
gap_closure: true
must_haves:
  truths:
    - "用户重新打开 decision tab 时，可以读回已保存决策卡。"
    - "用户可以在 decision 卡片中看到 linked messages，并点击回跳验证上下文。"
    - "系统会从 digest/summary 生成 pending suggestions，并允许 accept/reject 后保留审计结果。"
  artifacts:
    - path: "src/features/chat/services/suggestionExtraction.ts"
      provides: "Deterministic summary-to-suggestion extraction contract"
      contains: "extractSuggestionsFromSummary"
    - path: "src/features/chat/stores/decisionStore.ts"
      provides: "Decision hydration plus digest-driven suggestion materialization"
      contains: "hydrateCards"
    - path: "src/features/chat/components/DecisionPanel.vue"
      provides: "Decision linked-message rendering and suggestion disposition UI"
      contains: "openLinkedMessage"
    - path: "tests/unit/services/suggestionExtraction.test.ts"
      provides: "Extraction contract coverage"
      contains: "extracts pending suggestions"
  key_links:
    - from: "src/features/chat/stores/decisionStore.ts"
      to: "src/shared/lib/knowledgeDb.ts"
      via: "listDecisionCards + listDigestEntries + saveDecisionCard"
      pattern: "listDecisionCards|listDigestEntries|saveDecisionCard"
    - from: "src/features/chat/stores/decisionStore.ts"
      to: "src/features/chat/services/suggestionExtraction.ts"
      via: "digest summary extraction pipeline"
      pattern: "extractSuggestionsFromSummary"
    - from: "src/features/chat/components/DecisionPanel.vue"
      to: "src/matrix/inbox.ts"
      via: "linked-message preload and focus navigation"
      pattern: "loadInboxEventContext"
---

<objective>
修复 decision 主链路：恢复已保存决策卡、补齐 linked-message 可追溯回跳，并把 digest/summary 提取为可审计的 pending suggestions。

Purpose: 关闭 DECI-01/02 的两个核心缺口——“决策可建不可追溯”与“只有 accept/reject 后半段，没有 suggestion 生成前半段”。
Output: suggestion extraction service、带 hydration/materialization 的 decisionStore、linked-message 可追溯的 DecisionPanel，以及对应测试。
</objective>

<execution_context>
@/Users/shikun/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/shikun/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-offline-digest-and-decision-capture/04-RESEARCH.md
@.planning/phases/04-offline-digest-and-decision-capture/04-VERIFICATION.md
@.planning/phases/04-offline-digest-and-decision-capture/04-03-SUMMARY.md
@src/shared/lib/knowledgeDb.ts
@src/features/chat/types/decision.ts
@src/features/chat/types/knowledge.ts
@src/features/chat/components/TaskPanel.vue

<interfaces>
From src/features/chat/types/decision.ts:
```typescript
export interface CreateDecisionCardInput {
  id: string
  conclusion: string
  context: string
  owner: string
  status: DecisionStatus
  citations: CitationRef[]
  suggestions?: CreateDecisionSuggestionInput[]
}
```

From src/shared/lib/knowledgeDb.ts:

```typescript
createKnowledgeRepository().listDecisionCards(status?)
createKnowledgeRepository().listDigestEntries(relevance?)
createKnowledgeRepository().saveDecisionCard(card)
createKnowledgeRepository().updateSuggestionDisposition(...)
```

From src/features/chat/components/TaskPanel.vue pattern:

```typescript
await loadInboxEventContext(roomId, eventId)
await router.push({ path: `/dm/${encodeURIComponent(roomId)}`, query: { focusEventId: eventId } })
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 先锁定 decision 回读、linked-message 回跳与 suggestion extraction 的失败测试</name>
  <files>tests/unit/services/suggestionExtraction.test.ts, tests/unit/stores/decisionStore.test.ts, tests/components/DecisionPanel.test.ts</files>
  <behavior>
    - Test 1: `extractSuggestionsFromSummary()` 会从 digest summary 产出带 citation 的 `action`/`blocker` pending suggestions；无匹配信号时返回空数组。
    - Test 2: `decisionStore.hydrateCards()` 会从 `knowledgeDb.listDecisionCards()` 恢复历史卡片，并从 `listDigestEntries()` materialize 缺失的 pending suggestions。
    - Test 3: DecisionPanel 渲染 decision citations / linked messages，并点击后复用 preload + `focusEventId` 跳转。
    - Test 4: accept/reject 不能被新的 digest materialization 覆盖，已存在 disposition 必须保留。
  </behavior>
  <action>新增 `suggestionExtraction` 单测，并扩展 decision store / panel 测试覆盖历史回读、digest → pending suggestion materialization、linked-message 渲染与跳转。**测试必须显式锁定“已接受/已拒绝 suggestion 不会在重新 materialize 时被重置回 pending”。** 保持 RED，不要先实现服务或 UI。</action>
  <verify>
    <automated>pnpm vitest run tests/unit/services/suggestionExtraction.test.ts tests/unit/stores/decisionStore.test.ts tests/components/DecisionPanel.test.ts</automated>
  </verify>
  <done>DECI-01/02 的缺口行为已测试化，当前失败集中在 extraction、hydrate 或 linked-message wiring 缺失。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现 digest suggestion 提取、decision 回读与 linked-message 追溯 UI</name>
  <files>src/features/chat/services/suggestionExtraction.ts, src/features/chat/stores/decisionStore.ts, src/features/chat/components/DecisionPanel.vue, tests/unit/services/suggestionExtraction.test.ts, tests/unit/stores/decisionStore.test.ts, tests/components/DecisionPanel.test.ts</files>
  <behavior>
    - Test 1: reopening decision tab restores saved cards.
    - Test 2: digest summaries produce stable pending suggestions with preserved citations and deterministic IDs.
    - Test 3: DecisionPanel shows linked messages plus suggestion source/disposition state and supports preload/fallback jump.
    - Test 4: accept/reject audit metadata remains the source of truth after rematerialization.
  </behavior>
  <action>创建 `src/features/chat/services/suggestionExtraction.ts`，导出 `extractSuggestionsFromSummary(entry)`：基于 digest title/summary 的本地确定性规则提取 `action`/`blocker` suggestions，保留原 citation，生成稳定 suggestion id（例如基于 `entry.id + kind + index`），没有命中时返回空数组。然后在 `decisionStore.ts` 增加 `hydrateCards()` 与 `materializeSuggestionsFromDigest()`：先读 `repository.listDecisionCards()`，再读 `repository.listDigestEntries()`；对每个有 extracted suggestions 的 digest entry upsert 一个稳定的“digest-backed decision draft”（例如 `decision:digest:${entry.id}`），把 digest 的 citations 作为 linked messages，已存在 suggestion 的 accepted/rejected disposition 必须保留，不得被新 extraction 覆盖。最后在 `DecisionPanel.vue` 挂载时调用 hydrate/materialize，渲染 linked-message 列表、suggestion 来源文案与 accept/reject 状态，并复用 `loadInboxEventContext + focusEventId` 实现回跳。禁止新增路由，禁止在组件内直接改写 suggestion disposition。</action>
  <verify>
    <automated>pnpm vitest run tests/unit/services/suggestionExtraction.test.ts tests/unit/stores/decisionStore.test.ts tests/components/DecisionPanel.test.ts</automated>
  </verify>
  <done>DECI-01/02 缺口关闭：决策卡可恢复、可追溯，digest/summary 也能自动产出并持久化 pending suggestions。</done>
</task>

</tasks>

<verification>
运行：`pnpm vitest run tests/unit/services/suggestionExtraction.test.ts tests/unit/stores/decisionStore.test.ts tests/components/DecisionPanel.test.ts`。
</verification>

<success_criteria>

- DECI-01：decision tab 可恢复历史决策卡，并能从 linked messages 回跳原始上下文。
- DECI-02：digest/summary 会生成 pending suggestions，用户可 accept/reject 且审计结果不会丢失。
  </success_criteria>

<output>
After completion, create `.planning/phases/04-offline-digest-and-decision-capture/04-07-SUMMARY.md`
</output>
