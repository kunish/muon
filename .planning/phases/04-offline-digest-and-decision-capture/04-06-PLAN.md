---
phase: 04-offline-digest-and-decision-capture
plan: "06"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/chat/types/digest.ts
  - src/matrix/digest.ts
  - src/features/chat/stores/digestStore.ts
  - src/features/chat/components/OfflineDigestPanel.vue
  - tests/unit/stores/digestStore.test.ts
  - tests/components/OfflineDigestPanel.test.ts
autonomous: true
requirements: [DIGE-01, DIGE-02, DIGE-03]
gap_closure: true
must_haves:
  truths:
    - "用户重新打开 Knowledge 面板时，digest tab 会先读回已保存 digest，再刷新当前离线窗口摘要。"
    - "用户回归后能看到离线窗口内由真实 Matrix 消息生成的 digest 条目。"
    - "digest 条目按 responsibility > follow > mention 稳定排序，且 follow 来自 pinned/favourite room 映射。"
    - "digest citation 继续复用 preload + focusEventId 回跳链路。"
  artifacts:
    - path: "src/features/chat/stores/digestStore.ts"
      provides: "Digest hydration, Matrix event ingestion, and away-window build orchestration"
      contains: "initializeDigest"
    - path: "src/matrix/digest.ts"
      provides: "Digest materializer that derives real relevance from room/user signals"
      contains: "materializeOfflineDigest"
    - path: "src/features/chat/components/OfflineDigestPanel.vue"
      provides: "Digest panel mount-time restore/refresh behavior"
      contains: "onMounted"
    - path: "tests/unit/stores/digestStore.test.ts"
      provides: "Coverage for hydration, event ingestion, and relevance ordering"
      contains: "hydrates saved digest entries"
  key_links:
    - from: "src/features/chat/stores/digestStore.ts"
      to: "src/shared/lib/knowledgeDb.ts"
      via: "listDigestEntries + saveDigestEntry"
      pattern: "listDigestEntries|saveDigestEntry"
    - from: "src/features/chat/stores/digestStore.ts"
      to: "src/matrix/events.ts"
      via: "room.message runtime listener"
      pattern: "matrixEvents\\.(on|off)"
    - from: "src/matrix/digest.ts"
      to: "src/matrix/rooms.ts"
      via: "room summary relevance mapping"
      pattern: "isPinned|highlightCount"
    - from: "src/features/chat/components/OfflineDigestPanel.vue"
      to: "src/matrix/inbox.ts"
      via: "citation preload and focus navigation"
      pattern: "loadInboxEventContext"
---

<objective>
修复 digest 运行时主链路：Knowledge 面板打开时能恢复历史 digest、订阅真实消息、按真实相关性生成并展示 away-window 摘要。

Purpose: 关闭 Phase 4 中 DIGE-01/02/03 的三处阻塞点（只写不读、无运行时接线、相关性仍靠手工 hint）。
Output: 带回读/订阅/真实 relevance 计算的 digest store、materializer、panel，以及对应测试更新。
</objective>

<execution_context>
@/Users/shikun/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/shikun/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-offline-digest-and-decision-capture/04-RESEARCH.md
@.planning/phases/04-offline-digest-and-decision-capture/04-VERIFICATION.md
@.planning/phases/04-offline-digest-and-decision-capture/04-02-SUMMARY.md
@src/shared/lib/knowledgeDb.ts
@src/shared/composables/useNetworkStatus.ts
@src/matrix/events.ts
@src/matrix/rooms.ts
@src/matrix/index.ts

<interfaces>
From src/shared/lib/knowledgeDb.ts:
```typescript
createKnowledgeRepository().listDigestEntries(relevance?)
createKnowledgeRepository().saveDigestEntry(entry)
```

From src/matrix/events.ts:
```typescript
export const matrixEvents: Emitter<{
  'room.message': { roomId: string, event: any }
  'sync.state': { state: string }
}>
```

From src/matrix/rooms.ts:
```typescript
export function getRoomSummaries(): RoomSummary[]
// RoomSummary exposes roomId, isPinned, highlightCount
```

From src/shared/composables/useNetworkStatus.ts:
```typescript
export function useNetworkStatus(): { status: Ref<ConnectionStatus>, lastOfflineAt: Ref<number | null> }
```
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 先锁定 digest 回读、运行时订阅与真实 relevance 的失败测试</name>
  <files>tests/unit/stores/digestStore.test.ts, tests/components/OfflineDigestPanel.test.ts</files>
  <behavior>
    - Test 1: `initializeDigest()` 会先从 `knowledgeDb.listDigestEntries()` 恢复已保存条目，再按当前窗口刷新 session。
    - Test 2: store 订阅 `matrixEvents.on('room.message')` 后，真实消息事件会被 ingest，并通过 `getRoomSummaries()` + 当前用户信号推导 relevance，而不是依赖外部 `relevanceHint`。
    - Test 3: `follow` 的临时真实来源固定为 `RoomSummary.isPinned === true`；`responsibility` 由 highlight/mention heuristics 产生；其余回落到 `mention`。
    - Test 4: `OfflineDigestPanel` 挂载时触发 digest 初始化；citation 点击仍先 preload 后用 `focusEventId` 导航，失败只 warning。
  </behavior>
  <action>扩展现有 digest store / panel 测试，mock `listDigestEntries`、`matrixEvents`、`getRoomSummaries`、`getClient().getUserId()` 与 router。**不要继续使用手工传入 `relevanceHint` 作为主断言来源；测试必须明确断言真实运行时映射规则。** 让测试先失败，失败点仅来自待补齐的 hydrate / runtime wiring / relevance logic。</action>
  <verify>
    <automated>pnpm vitest run tests/unit/stores/digestStore.test.ts tests/components/OfflineDigestPanel.test.ts</automated>
  </verify>
  <done>digest gap 的目标行为被测试化，且当前实现明确失败在回读、订阅或 relevance 推导缺口上。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现 digest 初始化、真实消息接线与 relevance materialization</name>
  <files>src/features/chat/types/digest.ts, src/matrix/digest.ts, src/features/chat/stores/digestStore.ts, src/features/chat/components/OfflineDigestPanel.vue, tests/unit/stores/digestStore.test.ts, tests/components/OfflineDigestPanel.test.ts</files>
  <behavior>
    - Test 1: Knowledge digest tab 重开时立即显示已保存 digest；若存在 `lastOfflineAt`，随后刷新当前离线窗口 session。
    - Test 2: `room.message` 事件会生成 `DigestSourceEvent` 并用真实 room/user 信号推导 `responsibility/follow/mention`。
    - Test 3: 可见 digest 继续按 `responsibility > follow > mention`、同级按时间倒序输出。
    - Test 4: citation jump 的 preload/fallback 行为保持不变。
  </behavior>
  <action>在 `digestStore.ts` 新增显式初始化入口（例如 `initializeDigest()` / `startRuntimeSync()` / `stopRuntimeSync()`），先调用 `repository.listDigestEntries()` 恢复历史，再在 `lastOfflineAt` 存在时调用 `buildDigestSession()`；用 `matrixEvents.on('room.message')` 接入真实消息流，`onUnmounted`/stop API 解除监听并避免重复绑定。更新 `src/matrix/digest.ts` 与 `src/features/chat/types/digest.ts`，用 `getRoomSummaries()` + 当前用户 ID 计算 relevance：**`follow` 固定映射 pinned/favourite room，`responsibility` 使用 highlight/mention heuristic，只有都不命中时才回落 `mention`。** `OfflineDigestPanel.vue` 挂载时调用初始化并保持原有 citation preload + fallback 导航。禁止引入新路由、禁止把 digest 继续建模成“测试手工喂数据才可见”的孤立链路。</action>
  <verify>
    <automated>pnpm vitest run tests/unit/stores/digestStore.test.ts tests/components/OfflineDigestPanel.test.ts</automated>
  </verify>
  <done>DIGE-01/02/03 缺口关闭：digest 能回读、会被真实消息刷新、并按真实 relevance 稳定展示与回跳。</done>
</task>

</tasks>

<verification>
运行：`pnpm vitest run tests/unit/stores/digestStore.test.ts tests/components/OfflineDigestPanel.test.ts`。
</verification>

<success_criteria>
- DIGE-01：Knowledge digest tab 打开即可恢复历史 digest，并在 away-window 存在时自动刷新当前摘要。
- DIGE-02：每条 digest 保持 citation 验证回跳能力。
- DIGE-03：digest relevance 来自真实 room/user 信号，而不是测试专用 hint。
</success_criteria>

<output>
After completion, create `.planning/phases/04-offline-digest-and-decision-capture/04-06-SUMMARY.md`
</output>
