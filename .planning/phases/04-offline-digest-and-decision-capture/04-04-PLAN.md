---
phase: 04-offline-digest-and-decision-capture
plan: "04"
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/features/chat/services/crossSessionQa.ts
  - src/features/chat/components/CrossSessionQaPanel.vue
  - src/locales/en.json
  - src/locales/zh.json
  - tests/unit/services/crossSessionQa.test.ts
  - tests/components/CrossSessionQaPanel.test.ts
autonomous: true
requirements: [DECI-03]
must_haves:
  truths:
    - "用户可以提跨会话问题并得到带 citations 的回答。"
    - "回答引用仅来自当前 joined-room 授权范围。"
    - "用户可逐条展开回答引用并跳回原始消息验证答案依据。"
  artifacts:
    - path: "src/features/chat/services/crossSessionQa.ts"
      provides: "Joined-room scoped cross-session Q&A service"
      contains: "askCrossSessionQuestion"
    - path: "src/features/chat/components/CrossSessionQaPanel.vue"
      provides: "Question input, cited answer rendering, and citation jump"
      contains: "answer.citations"
  key_links:
    - from: "src/features/chat/services/crossSessionQa.ts"
      to: "src/matrix/retrieval.ts"
      via: "joined-room bounded evidence retrieval"
      pattern: "searchRoomEvents"
    - from: "src/features/chat/components/CrossSessionQaPanel.vue"
      to: "src/matrix/inbox.ts"
      via: "citation preload + focus navigation"
      pattern: "loadInboxEventContext"
---

<objective>
交付跨会话问答主能力：用户可提出问题、获得带 citations 的回答，并逐条验证引用依据。

Purpose: 先独立完成 DECI-03 的服务/面板闭环，避免把 QA 能力与侧栏入口集成混成超大执行切片。
Output: crossSessionQa service、CrossSessionQaPanel、QA 文案与测试。
</objective>

<execution_context>
@/Users/shikun/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/shikun/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-offline-digest-and-decision-capture/04-RESEARCH.md
@src/matrix/retrieval.ts
@src/matrix/inbox.ts
@src/features/chat/components/GlobalSearch.vue

<interfaces>
From src/matrix/retrieval.ts:
```typescript
export async function searchRoomEvents(term: string, limit = 20): Promise<RetrievalPage>
```

From src/matrix/inbox.ts:
```typescript
export async function loadInboxEventContext(roomId: string, eventId: string, limit = 20): Promise<InboxEventContext>
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 先写 cross-session QA RED 测试（Wave 0）</name>
  <files>tests/unit/services/crossSessionQa.test.ts, tests/components/CrossSessionQaPanel.test.ts</files>
  <behavior>
    - Test 1: 提问后返回 answer + citations，且 citations 非空。
    - Test 2: QA 服务仅使用 joined-room 范围检索证据，left/invite 会话被过滤。
    - Test 3: CrossSessionQaPanel 支持 citation 点击回跳，预加载失败走 warning+降级导航。
  </behavior>
  <action>新增服务层与组件层测试文件，mock retrieval 与 router，先固定 DECI-03 的可追溯回答契约。若当前无 AI provider，使用 mock provider 合同测试（不阻塞本计划）。保持 RED。</action>
  <verify>
    <automated>pnpm vitest run tests/unit/services/crossSessionQa.test.ts tests/components/CrossSessionQaPanel.test.ts -t "citations|joined-room|fallback" -x</automated>
  </verify>
  <done>DECI-03 测试边界建立且测试先失败，失败集中在待实现服务/组件。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现 QA 服务与 CrossSessionQaPanel（GREEN）</name>
  <files>src/features/chat/services/crossSessionQa.ts, src/features/chat/components/CrossSessionQaPanel.vue, src/locales/en.json, src/locales/zh.json, tests/unit/services/crossSessionQa.test.ts, tests/components/CrossSessionQaPanel.test.ts</files>
  <behavior>
    - Test 1: askCrossSessionQuestion 返回结构化 answer，包含可追溯 citations。
    - Test 2: CrossSessionQaPanel 渲染问题输入、回答内容和 citation 列表。
    - Test 3: 点击 citation 复用 preload+fallback 导航链路。
  </behavior>
  <action>实现 `crossSessionQa.ts`：复用 `searchRoomEvents`（joined-room 限制）检索证据并生成带 citations 的回答结构；实现 `CrossSessionQaPanel.vue` 的问答输入、回答展示、citation 跳转（复用 `loadInboxEventContext + focusEventId`）。在本计划内仅补齐 QA 自身所需中英文文案，不处理侧栏入口/Knowledge 集成——这些放到后续独立计划完成，避免单计划文件过多。</action>
  <verify>
    <automated>pnpm vitest run tests/unit/services/crossSessionQa.test.ts tests/components/CrossSessionQaPanel.test.ts -x</automated>
  </verify>
  <done>DECI-03 主链路可用：用户可提问、获得带引用回答，并逐条回跳验证答案依据。</done>
</task>

</tasks>

<verification>
运行：`pnpm vitest run tests/unit/services/crossSessionQa.test.ts tests/components/CrossSessionQaPanel.test.ts && pnpm test:unit`。
</verification>

<success_criteria>
- DECI-03：跨会话问答返回答案时始终附带可点击 citations。
- 回答引用范围受 joined-room 权限边界约束。
- 用户可逐条打开回答引用验证答案依据。
</success_criteria>

<output>
After completion, create `.planning/phases/04-offline-digest-and-decision-capture/04-04-SUMMARY.md`
</output>
