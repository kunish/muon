---
phase: 04-offline-digest-and-decision-capture
plan: "05"
type: execute
wave: 4
depends_on: ["04-02", "04-03", "04-04"]
files_modified:
  - src/features/chat/components/KnowledgeCapturePanel.vue
  - src/features/chat/stores/chatStore.ts
  - src/features/chat/components/ChatWindow.vue
  - src/features/server/components/ChannelSidebar.vue
autonomous: true
requirements: [DIGE-01, DIGE-02, DIGE-03, DECI-01, DECI-02, DECI-03]
must_haves:
  truths:
    - "用户可从同一个 Knowledge 入口访问 digest、decision、qa 三个子视图。"
    - "Knowledge 面板接入现有 side-panel 状态机，不会把用户带到新的平行页面。"
    - "用户在 Knowledge 面板内切换 digest/decision/qa 时，已实现能力都保持可达。"
  artifacts:
    - path: "src/features/chat/components/KnowledgeCapturePanel.vue"
      provides: "Integrated Knowledge shell hosting digest/decision/qa panels"
      contains: "CrossSessionQaPanel"
    - path: "src/features/chat/stores/chatStore.ts"
      provides: "Side-panel state including knowledge"
      contains: "'knowledge'"
    - path: "src/features/server/components/ChannelSidebar.vue"
      provides: "Sidebar entry that opens the knowledge panel"
      contains: "knowledge"
    - path: "src/features/chat/components/ChatWindow.vue"
      provides: "Knowledge panel mount point in existing chat layout"
      contains: "KnowledgeCapturePanel"
  key_links:
    - from: "src/features/server/components/ChannelSidebar.vue"
      to: "src/features/chat/stores/chatStore.ts"
      via: "single side-panel state toggle for knowledge"
      pattern: "knowledge"
    - from: "src/features/chat/components/ChatWindow.vue"
      to: "src/features/chat/components/KnowledgeCapturePanel.vue"
      via: "existing side panel mount"
      pattern: "KnowledgeCapturePanel"
    - from: "src/features/chat/components/KnowledgeCapturePanel.vue"
      to: "OfflineDigestPanel/DecisionPanel/CrossSessionQaPanel"
      via: "tabbed panel integration"
      pattern: "activeTab"
---

<objective>
把 digest、decision、qa 三条能力汇聚到统一 Knowledge 入口，完成 Phase 4 的可发现性与单入口工作流。

Purpose: 避免用户需要记住多个入口或页面，确保离线知识回顾、决策沉淀、跨会话问答在同一侧栏容器中可达。
Output: Knowledge 面板集成、chatStore side-panel 扩展、ChatWindow 挂载、ChannelSidebar 入口接线。
</objective>

<execution_context>
@/Users/shikun/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/shikun/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-offline-digest-and-decision-capture/04-RESEARCH.md
@.planning/phases/04-offline-digest-and-decision-capture/04-02-SUMMARY.md
@.planning/phases/04-offline-digest-and-decision-capture/04-03-SUMMARY.md
@.planning/phases/04-offline-digest-and-decision-capture/04-04-SUMMARY.md
@src/features/chat/components/KnowledgeCapturePanel.vue
@src/features/chat/stores/chatStore.ts
@src/features/chat/components/ChatWindow.vue
@src/features/server/components/ChannelSidebar.vue

<interfaces>
From src/features/chat/stores/chatStore.ts:
```typescript
const activeSidePanel = ref<'threads' | 'search' | 'pinned' | 'starred' | 'members' | 'settings' | 'tasks' | null>(null)
```

From 04-01/02/03/04 outputs:
```typescript
// KnowledgeCapturePanel.vue should host these child panels:
OfflineDigestPanel
DecisionPanel
CrossSessionQaPanel
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 先写 Knowledge 入口集成 RED 测试</name>
  <files>tests/components/KnowledgeCapturePanel.integration.test.ts</files>
  <behavior>
    - Test 1: KnowledgeCapturePanel 集成 OfflineDigestPanel/DecisionPanel/CrossSessionQaPanel 三个 tab。
    - Test 2: chatStore 扩展 `knowledge` side-panel 状态后，ChatWindow 会挂载 KnowledgeCapturePanel。
    - Test 3: ChannelSidebar 的 knowledge 入口会打开既有 side-panel，而不是跳转到新页面。
  </behavior>
  <action>新增集成测试文件，mock digest/decision/qa 子面板与 chatStore，先固定单入口侧栏集成行为。测试聚焦已有布局和状态机接线，不写无关视觉快照。保持 RED。</action>
  <verify>
    <automated>pnpm vitest run tests/components/KnowledgeCapturePanel.integration.test.ts -x</automated>
  </verify>
  <done>Knowledge 单入口集成边界已测试化，当前失败仅来自待实现接线。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现 Knowledge 统一入口接线并转绿</name>
  <files>src/features/chat/components/KnowledgeCapturePanel.vue, src/features/chat/stores/chatStore.ts, src/features/chat/components/ChatWindow.vue, src/features/server/components/ChannelSidebar.vue, tests/components/KnowledgeCapturePanel.integration.test.ts</files>
  <behavior>
    - Test 1: KnowledgeCapturePanel 在同一容器内切换 digest/decision/qa 三个子视图。
    - Test 2: ChannelSidebar 可打开 `knowledge` 面板且不引入新路由页面。
    - Test 3: 已有 side-panel 模式（tasks/search 等）与 knowledge 不冲突。
  </behavior>
  <action>更新 `KnowledgeCapturePanel.vue` 为真实集成壳层，挂入 `OfflineDigestPanel`、`DecisionPanel`、`CrossSessionQaPanel` 三个 tab；在 `chatStore.ts` 增加 `knowledge` side-panel 状态；在 `ChatWindow.vue` 复用既有侧栏挂载逻辑渲染 Knowledge 面板；在 `ChannelSidebar.vue` 增加 Knowledge 入口按钮。禁止新增页面路由、禁止创建第二套侧栏状态机。</action>
  <verify>
    <automated>pnpm vitest run tests/components/KnowledgeCapturePanel.integration.test.ts -x</automated>
  </verify>
  <done>用户可通过统一 Knowledge 入口访问 digest、decision、qa，且全部接入既有 side-panel 交互模型。</done>
</task>

</tasks>

<verification>
运行：`pnpm vitest run tests/components/KnowledgeCapturePanel.integration.test.ts -x`。
</verification>

<success_criteria>
- 用户可从单一 Knowledge 入口进入 digest、decision、qa 三个子能力。
- Knowledge 面板完全复用既有 side-panel 状态机与挂载点。
- Phase 4 所有已实现能力在统一容器内保持可达。
</success_criteria>

<output>
After completion, create `.planning/phases/04-offline-digest-and-decision-capture/04-05-SUMMARY.md`
</output>
