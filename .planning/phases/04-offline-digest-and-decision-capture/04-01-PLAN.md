---
phase: 04-offline-digest-and-decision-capture
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/chat/types/knowledge.ts
  - src/shared/lib/knowledgeDb.ts
  - tests/unit/stores/knowledgeDb.test.ts
  - src/features/chat/components/KnowledgeCapturePanel.vue
autonomous: true
requirements: [DIGE-01, DIGE-02, DIGE-03, DECI-01, DECI-02, DECI-03]
must_haves:
  truths:
    - "Digest、Decision、QA 三个能力共享同一套 citation 数据合同。"
    - "离线知识数据可在本地按索引查询，不依赖 localStorage 全量扫描。"
    - "执行端有统一 Knowledge 面板容器可承载后续 digest/decision/qa 子面板。"
  artifacts:
    - path: "src/features/chat/types/knowledge.ts"
      provides: "Shared citation/domain contracts for digest/decision/qa"
      contains: "export interface CitationRef"
    - path: "src/shared/lib/knowledgeDb.ts"
      provides: "Dexie schema + repository helpers for offline knowledge entities"
      contains: "db.version(1).stores"
    - path: "src/features/chat/components/KnowledgeCapturePanel.vue"
      provides: "Single side-panel shell for offline digest/decision/qa tabs"
      contains: "defineProps"
    - path: "tests/unit/stores/knowledgeDb.test.ts"
      provides: "Contract tests for Dexie schema and repository invariants"
      contains: "MuonKnowledgeDB"
  key_links:
    - from: "src/shared/lib/knowledgeDb.ts"
      to: "dexie"
      via: "versioned indexed schema"
      pattern: "version\(1\)\.stores"
    - from: "src/features/chat/types/knowledge.ts"
      to: "digest/decision/qa stores"
      via: "shared CitationRef and enum contracts"
      pattern: "CitationRef"
    - from: "src/features/chat/components/KnowledgeCapturePanel.vue"
      to: "future OfflineDigestPanel/DecisionPanel/CrossSessionQaPanel"
      via: "tab slot/conditional render contract"
      pattern: "activeTab"
---

<objective>
先构建 Phase 4 的共享底座：统一 citation 合同、Dexie 持久化与 Knowledge 面板壳层，避免后续 digest/decision/qa 各自发明数据模型和入口。

Purpose: 锁定“citation-first + indexed offline store + single panel entry”三条基础约束，降低并行实现阶段的返工概率。
Output: knowledge 类型合同、Dexie 数据层、KnowledgeCapturePanel 壳组件和基础单测护栏。
</objective>

<execution_context>
@/Users/shikun/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/shikun/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-offline-digest-and-decision-capture/04-RESEARCH.md
@src/matrix/inbox.ts
@src/features/chat/stores/retrievalStore.ts

<interfaces>
From src/matrix/inbox.ts:
```typescript
export async function loadInboxEventContext(roomId: string, eventId: string, limit = 20): Promise<InboxEventContext>
```

From src/matrix/retrieval.ts:
```typescript
export interface RetrievalItem {
  roomId: string
  eventId: string
  body: string
  sender: string
  ts: number
  rank: number
}
```

From src/features/chat/stores/taskStore.ts (state transition discipline to reuse):
```typescript
function transitionStatus(id: string, to: TaskStatus)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 建立共享知识合同与 Knowledge DB 的 RED 测试</name>
  <files>src/features/chat/types/knowledge.ts, src/shared/lib/knowledgeDb.ts, tests/unit/stores/knowledgeDb.test.ts</files>
  <behavior>
    - Test 1: CitationRef 必须包含 roomId/eventId，且空引用集合会被 schema 校验拒绝。
    - Test 2: digest/decision/qa 三类实体都能通过 Dexie 索引字段按 createdAt 或 status 查询。
    - Test 3: suggestion disposition 默认 pending，只有 accepted/rejected 两种显式用户决策。
  </behavior>
  <action>先定义 `knowledge.ts` 的领域接口与 zod schema（CitationRef、DigestEntry、DecisionCard、SuggestionDisposition、CrossSessionQaAnswer），再在 `tests/unit/stores/knowledgeDb.test.ts` 写失败测试固定索引与默认状态约束。随后创建 `knowledgeDb.ts` 但保持最小实现，确保当前测试仍处于 RED。禁止在此任务引入 localStorage 回退逻辑。</action>
  <verify>
    <automated>pnpm vitest run tests/unit/stores/knowledgeDb.test.ts -x</automated>
  </verify>
  <done>共享领域约束与索引期望被测试明确表达，测试先失败且失败原因与合同缺口一致。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现 Dexie 持久化仓储与 Knowledge 面板壳层（GREEN）</name>
  <files>src/shared/lib/knowledgeDb.ts, src/features/chat/components/KnowledgeCapturePanel.vue, tests/unit/stores/knowledgeDb.test.ts</files>
  <behavior>
    - Test 1: Dexie schema version(1) 的 stores 字段覆盖 digestEntries/decisions/qaSessions 与 citation 索引。
    - Test 2: repository helpers 支持按 relevance/status/time 查询并返回稳定排序。
    - Test 3: KnowledgeCapturePanel 暴露 digest/decision/qa 三个 tab 插槽位（可先占位文案）。
  </behavior>
  <action>补全 `knowledgeDb.ts`：使用 Dexie v4 建立 `MuonKnowledgeDB`，实现最小 repository API（insert/list/updateDisposition 等）并让 Task 1 测试转绿。新增 `KnowledgeCapturePanel.vue` 作为统一侧栏容器，定义 `activeTab` 状态与三个子面板挂载位（当前先占位，不实现业务逻辑）。必须沿用单入口 side-panel 策略，不创建新路由页面。</action>
  <verify>
    <automated>pnpm vitest run tests/unit/stores/knowledgeDb.test.ts -x</automated>
  </verify>
  <done>Dexie 数据层可用且测试通过，KnowledgeCapturePanel 为后续并行计划提供稳定挂载合同。</done>
</task>

</tasks>

<verification>
运行：`pnpm vitest run tests/unit/stores/knowledgeDb.test.ts`。
</verification>

<success_criteria>
- 所有 Phase 4 子能力共享 citation-first 类型合同。
- 本地知识数据写入/查询走 Dexie 索引，不走 localStorage 扫描。
- 存在可复用的 Knowledge 统一面板壳层，后续计划无需重建入口。
</success_criteria>

<output>
After completion, create `.planning/phases/04-offline-digest-and-decision-capture/04-01-SUMMARY.md`
</output>
