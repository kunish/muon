---
phase: 06-search-surface-integration-and-retrieval-completion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/components/ChatWindow.search.integration.test.ts
  - tests/components/GlobalSearch.test.ts
  - tests/components/GlobalSearch.performance.test.ts
autonomous: true
requirements:
  - RETR-01
  - RETR-02
  - RELI-02
must_haves:
  truths:
    - "Automated coverage proves the real chat header search trigger mounts the cross-conversation retrieval panel."
    - "Automated coverage proves reachable search jumps use canonical /dm navigation with focusEventId after bounded preload."
    - "Automated coverage proves joined-room filtering and pagination stay enforced from the real ChatWindow mount path."
  artifacts:
    - path: "tests/components/ChatWindow.search.integration.test.ts"
      provides: "Reachable mount-point coverage for ChatHeader -> ChatWindow -> search panel"
      min_lines: 120
    - path: "tests/components/GlobalSearch.test.ts"
      provides: "Canonical navigation and close-behavior regression coverage"
      contains: "/dm/"
    - path: "tests/components/GlobalSearch.performance.test.ts"
      provides: "Bounded preload and paginated large-result regression coverage"
      contains: "global-search-hit"
  key_links:
    - from: "tests/components/ChatWindow.search.integration.test.ts"
      to: "src/features/chat/components/ChatWindow.vue"
      via: "mounted side-panel search flow"
      pattern: "activeSidePanel === 'search'"
    - from: "tests/components/GlobalSearch.test.ts"
      to: "src/features/chat/components/GlobalSearch.vue"
      via: "router push assertion"
      pattern: "/dm/"
    - from: "tests/components/GlobalSearch.performance.test.ts"
      to: "src/features/chat/stores/retrievalStore.ts"
      via: "paginated result-session assertions"
      pattern: "canLoadMore|loadMore"
---

<objective>
Lock the reachable Phase 6 search contract before refactoring the production search surface.

Purpose: Prevent another split-brain implementation by proving the real header-triggered mount path, canonical jump routing, and joined-room/performance guarantees in tests first.
Output: A new ChatWindow-level integration test plus updated GlobalSearch regression tests that describe the intended production behavior.
</objective>

<execution_context>
@/Users/shikun/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/shikun/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-search-surface-integration-and-retrieval-completion/06-RESEARCH.md
@.planning/phases/06-search-surface-integration-and-retrieval-completion/06-VALIDATION.md
@.planning/phases/03-cross-conversation-retrieval/03-02-SUMMARY.md
@.planning/phases/05-reliability-and-performance-consistency/05-02-SUMMARY.md
@src/features/chat/components/ChatHeader.vue
@src/features/chat/components/ChatWindow.vue
@src/features/chat/components/GlobalSearch.vue
@src/features/chat/stores/chatStore.ts
@src/features/chat/stores/retrievalStore.ts

<interfaces>
From `src/features/chat/stores/chatStore.ts`:
```ts
const activeSidePanel = ref<'threads' | 'search' | 'pinned' | 'starred' | 'members' | 'settings' | 'tasks' | 'knowledge' | null>(null)
function toggleSidePanel(panel: 'threads' | 'search' | 'pinned' | 'starred' | 'members' | 'settings' | 'tasks' | 'knowledge')
function closeSidePanel()
```

From `src/features/chat/stores/retrievalStore.ts`:
```ts
async function search(nextQuery: string)
async function loadMore()
function resetState(nextQuery = '')
const results = shallowRef<RetrievalItem[]>([])
const canLoadMore = ref(false)
```

From `src/features/chat/components/GlobalSearch.vue`:
```ts
const emit = defineEmits<{ close: [] }>()
await router.push({ path: `/chat/${encodeURIComponent(roomId)}`, query: { focusEventId: eventId } })
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a real ChatWindow search integration test</name>
  <files>tests/components/ChatWindow.search.integration.test.ts</files>
  <action>Create a new integration test file that mounts the real `ChatWindow.vue` shell with lightweight stubs for unrelated heavy children, clicks the existing `ChatHeader` search trigger, and proves the reachable path now expects the cross-conversation retrieval panel rather than room-scoped `SearchMessages`. Cover open, submit, joined-room-only render, load-more continuity, close, and result jump behavior from the mounted side panel. Mock router, Matrix client, `loadInboxEventContext`, and retrieval store dependencies the same way existing GlobalSearch tests do; do not introduce a second fake state machine outside `chatStore`.</action>
  <verify>
    <automated>pnpm vitest run tests/components/ChatWindow.search.integration.test.ts</automated>
  </verify>
  <done>`tests/components/ChatWindow.search.integration.test.ts` fails before implementation and explicitly describes the real production mount contract for RETR-01, RETR-02, and RELI-02.</done>
</task>

<task type="auto">
  <name>Task 2: Tighten existing GlobalSearch regressions around canonical routing and bounded behavior</name>
  <files>tests/components/GlobalSearch.test.ts, tests/components/GlobalSearch.performance.test.ts</files>
  <action>Update the existing component/performance tests so the expected navigation target is canonical `/dm/${encodeURIComponent(roomId)}` with `focusEventId`, not legacy `/chat/...`. Keep the existing joined-room filtering and virtualization assertions, and add explicit expectations that the reachable search surface preserves the 250ms preload budget and closes cleanly after room/result selection. Reuse current test helpers; do not add watch-mode flags or broad full-suite commands inside the tests.</action>
  <verify>
    <automated>pnpm vitest run tests/components/GlobalSearch.test.ts tests/components/GlobalSearch.performance.test.ts</automated>
  </verify>
  <done>The GlobalSearch regression suite encodes Phase 6’s canonical `/dm` jump contract, bounded preload behavior, and joined-room-safe pagination expectations.</done>
</task>

</tasks>

<verification>
Run `pnpm vitest run tests/components/ChatWindow.search.integration.test.ts tests/components/GlobalSearch.test.ts tests/components/GlobalSearch.performance.test.ts` before handing off. The new ChatWindow integration test must go RED against the current broken mount path.</verification>

<success_criteria>
- The real header-triggered search mount path is covered by automated tests.
- Canonical `/dm` + `focusEventId` navigation is asserted in retrieval tests.
- Joined-room filtering, load-more continuity, and bounded preload remain enforced in reachable-flow coverage.
</success_criteria>

<output>
After completion, create `.planning/phases/06-search-surface-integration-and-retrieval-completion/06-01-SUMMARY.md`
</output>
