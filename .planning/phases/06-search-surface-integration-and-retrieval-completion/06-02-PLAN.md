---
phase: 06-search-surface-integration-and-retrieval-completion
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - src/features/chat/components/GlobalSearch.vue
  - src/features/chat/components/ChatWindow.vue
  - src/features/chat/components/SearchMessages.vue
  - src/features/chat/stores/retrievalStore.ts
autonomous: true
requirements:
  - RETR-01
  - RETR-02
  - RELI-02
must_haves:
  truths:
    - "User opens the existing chat search trigger and sees the real cross-conversation retrieval panel inside the side panel."
    - "User can search, paginate, and jump to a result from that reachable panel without navigation stalling."
    - "User never sees hits from rooms they have left, even when stale data exists in the retrieval session."
  artifacts:
    - path: "src/features/chat/components/GlobalSearch.vue"
      provides: "Side-panel-native cross-conversation search UI"
      min_lines: 180
    - path: "src/features/chat/components/ChatWindow.vue"
      provides: "Reachable ChatWindow mount wiring for the real retrieval panel"
      contains: "GlobalSearch"
    - path: "src/features/chat/stores/retrievalStore.ts"
      provides: "Explicit retrieval session reset policy for open/close lifecycle"
      contains: "resetState"
  key_links:
    - from: "src/features/chat/components/ChatWindow.vue"
      to: "src/features/chat/components/GlobalSearch.vue"
      via: "activeSidePanel === 'search' inline mount"
      pattern: "GlobalSearch"
    - from: "src/features/chat/components/GlobalSearch.vue"
      to: "src/features/chat/stores/retrievalStore.ts"
      via: "submit/load-more/reset actions"
      pattern: "retrievalStore\.(search|loadMore|resetState)"
    - from: "src/features/chat/components/GlobalSearch.vue"
      to: "src/app/router/index.ts"
      via: "canonical room push with focusEventId"
      pattern: "/dm/"
---

<objective>
Replace the reachable room-scoped search surface with the production cross-conversation retrieval implementation.

Purpose: Close the audit gap so the real chat header search entrypoint finally delivers Phase 3 retrieval functionality under Phase 5 performance constraints.
Output: An inline side-panel GlobalSearch implementation, explicit retrieval-session reset behavior, and ChatWindow wiring that no longer depends on legacy `SearchMessages` behavior.
</objective>

<execution_context>
@/Users/shikun/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/shikun/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-search-surface-integration-and-retrieval-completion/06-RESEARCH.md
@.planning/phases/06-search-surface-integration-and-retrieval-completion/06-VALIDATION.md
@.planning/phases/03-cross-conversation-retrieval/03-02-SUMMARY.md
@.planning/phases/05-reliability-and-performance-consistency/05-02-SUMMARY.md
@src/features/chat/components/ChatWindow.vue
@src/features/chat/components/GlobalSearch.vue
@src/features/chat/components/SearchMessages.vue
@src/features/chat/stores/chatStore.ts
@src/features/chat/stores/retrievalStore.ts
@src/app/router/index.ts

<interfaces>
From `src/matrix/retrieval.ts`:
```ts
export interface RetrievalItem {
  roomId: string
  eventId: string
  body: string
  sender: string
  ts: number
  rank: number
}

export interface RetrievalSession {
term: string
searchResults: ISearchResults
items: RetrievalItem[]
seenEventIds: Set<string>
allowedRoomIds: Set<string>
}

````

From `src/features/chat/stores/retrievalStore.ts`:
```ts
async function search(nextQuery: string)
async function loadMore()
function resetState(nextQuery = '')
const results = shallowRef<RetrievalItem[]>([])
const loading = ref(false)
const loadingMore = ref(false)
const canLoadMore = ref(false)
````

From `src/features/chat/stores/chatStore.ts`:

```ts
const activeSidePanel = ref<'threads' | 'search' | 'pinned' | 'starred' | 'members' | 'settings' | 'tasks' | 'knowledge' | null>(null)
function toggleSidePanel(panel: 'threads' | 'search' | 'pinned' | 'starred' | 'members' | 'settings' | 'tasks' | 'knowledge')
function closeSidePanel()
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refactor GlobalSearch into the inline side-panel retrieval surface</name>
  <files>src/features/chat/components/GlobalSearch.vue, src/features/chat/stores/retrievalStore.ts</files>
  <behavior>
    - Opening the reachable search panel starts from a fresh retrieval session instead of stale results/query state.
    - Submitting and loading more still use `retrievalStore.search()` / `loadMore()` with joined-room filtering preserved in rendered hits.
    - Room selection and message-result jumps navigate directly to canonical `/dm/${encodeURIComponent(roomId)}`; message jumps still race preload against the 250ms budget before closing the panel.
  </behavior>
  <action>Refactor `GlobalSearch.vue` so it renders naturally inside the existing 320px ChatWindow side-panel shell instead of teleporting to `body`. Keep the current split between room quick results and virtualized message hits, but adapt layout/classes for the narrower inline panel. Move query lifecycle ownership into `retrievalStore` (or otherwise keep query/results reset together) so opening/closing the panel always yields an intentional fresh session. Preserve joined-room filtering, `@tanstack/vue-virtual` usage with the jsdom-safe fallback slice, and the bounded preload warning path. Do not reintroduce room-scoped SDK search or search-on-type behavior.</action>
  <verify>
    <automated>pnpm vitest run tests/components/GlobalSearch.test.ts tests/components/GlobalSearch.performance.test.ts</automated>
  </verify>
  <done>`GlobalSearch.vue` works as an embedded side-panel retrieval surface, uses canonical `/dm` navigation, and exposes a deterministic reset policy through `retrievalStore`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewire ChatWindow to the real retrieval surface and retire the legacy reachable search path</name>
  <files>src/features/chat/components/ChatWindow.vue, src/features/chat/components/SearchMessages.vue</files>
  <behavior>
    - Clicking the existing ChatHeader search trigger mounts `GlobalSearch` in the side panel instead of the old room-only search component.
    - Closing the search panel tears down the reachable search session cleanly and reopening does not show stale hits.
    - No production-reachable path still depends on `SearchMessages.vue` for Phase 6 search behavior.
  </behavior>
  <action>Update `ChatWindow.vue` so `activeSidePanel === 'search'` renders the inline `GlobalSearch` component and routes its close behavior through the existing `chatStore.closeSidePanel()` coordinator. Remove `SearchMessages.vue` from the reachable flow: delete the file if nothing else imports it, or reduce it to a non-reachable compatibility wrapper only if deletion would break unrelated code. Keep the side-panel state machine as the only coordinator; do not add a modal-specific store or new search route/page.</action>
  <verify>
    <automated>pnpm vitest run tests/components/ChatWindow.search.integration.test.ts tests/components/GlobalSearch.test.ts tests/components/GlobalSearch.performance.test.ts</automated>
  </verify>
  <done>The real chat header search entrypoint now mounts the cross-conversation retrieval surface, legacy room-scoped search is no longer reachable, and all targeted Phase 6 regressions pass.</done>
</task>

</tasks>

<verification>
Run `pnpm vitest run tests/components/ChatWindow.search.integration.test.ts tests/components/GlobalSearch.test.ts tests/components/GlobalSearch.performance.test.ts` after implementation, then run `pnpm test:unit && pnpm test:e2e` before phase handoff.</verification>

<success_criteria>

- ChatWindow’s reachable search path uses the cross-conversation retrieval panel, not `SearchMessages`.
- Search result and room jumps use canonical `/dm` navigation while preserving `focusEventId` and the 250ms preload budget.
- Retrieval reopen behavior is deterministic and does not leak stale results.
  </success_criteria>

<output>
After completion, create `.planning/phases/06-search-surface-integration-and-retrieval-completion/06-02-SUMMARY.md`
</output>
