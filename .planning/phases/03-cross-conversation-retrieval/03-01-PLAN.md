---
phase: 03-cross-conversation-retrieval
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/matrix/retrieval.ts
  - src/matrix/index.ts
  - tests/unit/matrix/retrieval.test.ts
  - tests/mocks/matrix-search.ts
autonomous: true
requirements: [RETR-01, RETR-02]
must_haves:
  truths:
    - "用户输入关键词后，系统会返回跨会话消息检索结果。"
    - "检索结果只来自当前 membership 为 join 的会话。"
    - "检索支持分页续取，不会重复第一页结果。"
  artifacts:
    - path: "src/matrix/retrieval.ts"
      provides: "Cross-conversation retrieval service with membership scoping"
      contains: "searchRoomEvents"
    - path: "tests/unit/matrix/retrieval.test.ts"
      provides: "Unit coverage for query transform, joined-room filter, pagination"
      contains: "joined-room scope"
    - path: "tests/mocks/matrix-search.ts"
      provides: "Deterministic Matrix search mock payloads"
      contains: "next_batch"
    - path: "src/matrix/index.ts"
      provides: "Public export of retrieval service API"
      contains: "from './retrieval'"
  key_links:
    - from: "src/matrix/retrieval.ts"
      to: "matrix-js-sdk MatrixClient.searchRoomEvents"
      via: "keyword search request"
      pattern: "searchRoomEvents"
    - from: "src/matrix/retrieval.ts"
      to: "current joined rooms"
      via: "client.getRooms().filter(getMyMembership() === 'join')"
      pattern: "getMyMembership\(\) === 'join'"
    - from: "src/matrix/retrieval.ts"
      to: "Matrix pagination"
      via: "backPaginateRoomEventsSearch"
      pattern: "backPaginateRoomEventsSearch"
---

<objective>
先交付可复用的 Matrix 跨会话检索服务与单元测试护栏，保证后续 UI 接入时检索语义和权限边界已被固定。

Purpose: 将 RETR-01/RETR-02 的核心风险（检索正确性 + 授权范围）收敛在服务层，避免组件层重复拼接协议细节。
Output: retrieval service、公开导出、可复用 mock、以及覆盖 joined-room scope + pagination 的单测。
</objective>

<execution_context>
@/Users/shikun/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/shikun/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-cross-conversation-retrieval/03-RESEARCH.md
@src/features/chat/components/SearchMessages.vue
@src/features/chat/components/GlobalSearch.vue
@src/matrix/rooms.ts
@src/matrix/inbox.ts
@src/matrix/index.ts

<interfaces>
From src/matrix/inbox.ts:
```typescript
export async function loadInboxEventContext(roomId: string, eventId: string, limit = 20): Promise<InboxEventContext>
```

From src/features/chat/components/SearchMessages.vue (current baseline):
```typescript
const res = await client.searchRoomEvents({ term: q, filter: { rooms: [props.roomId] } } as any)
```

From src/features/chat/components/GlobalSearch.vue (joined-room convention):
```typescript
const all = client.getRooms().filter(r => r.getMyMembership() === 'join')
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 建立 retrieval 服务合同 + Wave 0 单测与 mock（先红灯）</name>
  <files>src/matrix/retrieval.ts, tests/unit/matrix/retrieval.test.ts, tests/mocks/matrix-search.ts</files>
  <behavior>
    - Test 1: 输入关键词时，服务调用 `searchRoomEvents` 并把 `term` 原样传入。
    - Test 2: 请求 filter.rooms 仅包含当前 joined rooms，left/invite 房间不会进入请求。
    - Test 3: 当返回 `next_batch` 时，分页函数可继续拉取并追加非重复结果。
    - Test 4: 空关键词直接返回空结果，不触发 SDK 请求。
  </behavior>
  <action>先在 `src/matrix/retrieval.ts` 定义并导出检索合同（结果类型、search 会话状态、分页函数签名），同时创建 `tests/mocks/matrix-search.ts` 提供含 rank、next_batch、mixed membership 的可复用 payload。随后在 `tests/unit/matrix/retrieval.test.ts` 写失败用例覆盖 RETR-01/RETR-02 的关键行为。不要在此任务实现完整业务逻辑（保持 RED 状态）。</action>
  <verify>
    <automated>pnpm vitest run tests/unit/matrix/retrieval.test.ts -t "cross-room search|joined-room scope|pagination"</automated>
  </verify>
  <done>服务接口与测试边界明确，测试在实现前失败且失败原因与需求一致。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现 joined-room 受限检索与分页续取（转绿灯）</name>
  <files>src/matrix/retrieval.ts, src/matrix/index.ts, tests/unit/matrix/retrieval.test.ts</files>
  <behavior>
    - Test 1: 检索结果按 SDK 返回顺序映射，包含 roomId/eventId/body/sender/ts/rank。
    - Test 2: `joined-room` 过滤强制生效，即使 SDK 可返回 left room 结果也不会请求这些房间。
    - Test 3: 连续调用分页函数时能推进 `next_batch`，无 token 时返回“不可继续分页”。
  </behavior>
  <action>实现 retrieval service：使用 `getClient()` + `searchRoomEvents` 执行跨会话检索，始终以 `client.getRooms().filter(r => r.getMyMembership() === 'join')` 构造 `filter.rooms`（即便研究提示其它可选策略，也必须按此约束以满足 RETR-02）。在同文件实现分页续取（`backPaginateRoomEventsSearch`）并输出稳定映射结构；最后在 `src/matrix/index.ts` 统一导出。禁止引入自建排序算法、禁止本阶段做本地全文索引。</action>
  <verify>
    <automated>pnpm vitest run tests/unit/matrix/retrieval.test.ts</automated>
  </verify>
  <done>retrieval 服务通过单测，且对外有稳定导出可供 UI 层直接消费。</done>
</task>

</tasks>

<verification>
运行：`pnpm vitest run tests/unit/matrix/retrieval.test.ts`。
</verification>

<success_criteria>
- RETR-01：服务层可跨会话返回关键词匹配结果并支持分页。
- RETR-02：请求范围被严格限制为当前 joined 房间。
- 组件层不需要直接操作 Matrix 搜索协议细节。
</success_criteria>

<output>
After completion, create `.planning/phases/03-cross-conversation-retrieval/03-01-SUMMARY.md`
</output>
