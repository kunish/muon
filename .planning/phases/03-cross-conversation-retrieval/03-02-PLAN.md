---
phase: 03-cross-conversation-retrieval
plan: "02"
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/features/chat/stores/retrievalStore.ts
  - src/features/chat/components/GlobalSearch.vue
  - tests/components/GlobalSearch.test.ts
  - src/locales/en.json
  - src/locales/zh.json
autonomous: true
requirements: [RETR-01, RETR-02]
must_haves:
  truths:
    - "用户可以在全局搜索入口输入关键词并看到跨会话消息结果。"
    - "用户点击某条结果后能回到对应会话并定位到目标消息。"
    - "UI 不会展示来自 left/invite 会话的检索结果。"
  artifacts:
    - path: "src/features/chat/stores/retrievalStore.ts"
      provides: "Query/result/loading/pagination state for global retrieval"
      contains: "search\(|loadMore\("
    - path: "src/features/chat/components/GlobalSearch.vue"
      provides: "Global retrieval UI with room + message result sections"
      contains: "focusEventId"
    - path: "tests/components/GlobalSearch.test.ts"
      provides: "Component coverage for render/exclude-left-room/jump behavior"
      contains: "jump to result"
  key_links:
    - from: "src/features/chat/components/GlobalSearch.vue"
      to: "src/features/chat/stores/retrievalStore.ts"
      via: "search submit and result rendering"
      pattern: "useRetrievalStore"
    - from: "src/features/chat/components/GlobalSearch.vue"
      to: "src/matrix/inbox.ts"
      via: "preload context before route push"
      pattern: "loadInboxEventContext"
    - from: "src/features/chat/components/GlobalSearch.vue"
      to: "router query"
      via: "focus event navigation"
      pattern: "focusEventId"
---

<objective>
把跨会话检索能力接入现有全局搜索入口，形成“搜索→结果→跳转定位”可用闭环，并保持权限边界与既有回跳链路一致。

Purpose: 将 03-01 的服务能力变成用户可直接使用的界面能力，满足 Phase 3 的可感知价值交付。
Output: retrieval store、GlobalSearch 消息检索结果 UI、点击结果回跳与组件测试。
</objective>

<execution_context>
@/Users/shikun/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/shikun/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-cross-conversation-retrieval/03-RESEARCH.md
@.planning/phases/03-cross-conversation-retrieval/03-01-SUMMARY.md
@src/features/chat/components/GlobalSearch.vue
@src/features/chat/components/SearchMessages.vue
@src/matrix/inbox.ts

<interfaces>
From 03-01 output (must consume directly, no re-invention):
```typescript
// src/matrix/retrieval.ts
export interface RetrievalHit {
  roomId: string
  eventId: string
  body: string
  sender: string
  ts: number
  rank?: number
}

export async function searchCrossConversationMessages(term: string, limit?: number): Promise<...>
export async function loadMoreCrossConversationMessages(session: ...): Promise<...>

````

Existing jump chain (must reuse):
```typescript
// src/matrix/inbox.ts
export async function loadInboxEventContext(roomId: string, eventId: string, limit = 20): Promise<InboxEventContext>
````

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 先写 GlobalSearch 检索渲染与回跳组件测试（RED）</name>
  <files>tests/components/GlobalSearch.test.ts</files>
  <behavior>
    - Test 1: 输入关键词后展示消息检索结果列表（含房间/消息摘要/时间）。
    - Test 2: left room 结果不会出现在渲染列表中。
    - Test 3: 点击结果先尝试 `loadInboxEventContext`，再路由到 `/chat/:roomId?focusEventId=:eventId`。
    - Test 4: 预加载失败时记录 warning 并降级导航，不阻断跳转。
  </behavior>
  <action>新增 `tests/components/GlobalSearch.test.ts`，mock retrieval store、router 与 `loadInboxEventContext`，先固定期望行为再进入实现。测试命名直接对应 RETR-01/RETR-02 与 jump chain 行为，避免写视觉快照类弱断言。</action>
  <verify>
    <automated>pnpm vitest run tests/components/GlobalSearch.test.ts</automated>
  </verify>
  <done>组件级行为边界已锁定，测试在实现前失败且可解释。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现 retrieval store 与 GlobalSearch 跨会话消息检索 UI（GREEN）</name>
  <files>src/features/chat/stores/retrievalStore.ts, src/features/chat/components/GlobalSearch.vue, src/locales/en.json, src/locales/zh.json, tests/components/GlobalSearch.test.ts</files>
  <behavior>
    - Test 1: store 管理 query/results/loading/error/next_batch 并可 loadMore。
    - Test 2: GlobalSearch 在“会话名检索”和“消息检索结果”之间提供清晰展示，不破坏原有房间跳转能力。
    - Test 3: 点击消息结果复用 `loadInboxEventContext + focusEventId` 链路并在成功/失败两路径都 close 弹层。
  </behavior>
  <action>实现 `retrievalStore`（集中管理搜索会话与分页状态），并改造 `GlobalSearch.vue`：保留现有房间名匹配能力，同时新增跨会话消息结果区与“加载更多”；消息项点击时严格复用既有回跳链路（先 preload context，后 `router.push` 带 `focusEventId`，失败仅 warning 后继续导航）。补齐中英文文案（搜索中、无结果、加密房间检索限制提示等），但不要在本计划引入新面板或新路由页面。</action>
  <verify>
    <automated>pnpm vitest run tests/components/GlobalSearch.test.ts</automated>
  </verify>
  <done>用户可在一个全局入口完成跨会话检索并跳回目标消息，组件测试覆盖成功/降级路径。</done>
</task>

</tasks>

<verification>
运行：`pnpm vitest run tests/components/GlobalSearch.test.ts && pnpm vitest run tests/unit/matrix/retrieval.test.ts`。
</verification>

<success_criteria>

- RETR-01：全局搜索支持跨会话关键词检索、结果展示和分页续取。
- RETR-02：UI 层不出现越权会话结果，且与服务层 joined-room 范围一致。
- 用户点击任一结果都可返回目标消息上下文（含预加载失败降级）。
  </success_criteria>

<output>
After completion, create `.planning/phases/03-cross-conversation-retrieval/03-02-SUMMARY.md`
</output>
