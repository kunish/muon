---
phase: 02-defer-and-message-to-task-loop
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/chat/types/defer.ts
  - src/features/chat/types/task.ts
  - src/features/chat/stores/deferStore.ts
  - src/features/chat/stores/taskStore.ts
  - tests/unit/stores/deferStore.test.ts
  - tests/unit/stores/taskStore.test.ts
autonomous: true
requirements: [INBX-04, INBX-05, TASK-01, TASK-03]
must_haves:
  truths:
    - "用户可以创建带提醒时间的 defer 事项并在系统中被稳定识别"
    - "defer 事项可以从 active 流转到 completed/archived 并从主队列移除"
    - "任务实体始终包含 assignee、dueAt、status 与源消息引用"
    - "任务状态只能在 todo/doing/done 三态内流转"
  artifacts:
    - path: "src/features/chat/types/defer.ts"
      provides: "DeferItem/DeferStatus/ReminderPreset 合同"
      exports: ["DeferStatus", "ReminderPreset", "DeferItem"]
    - path: "src/features/chat/types/task.ts"
      provides: "TaskItem/TaskStatus/TaskSourceRef 合同"
      exports: ["TaskStatus", "TaskItem", "TaskSourceRef"]
    - path: "src/features/chat/stores/deferStore.ts"
      provides: "defer 状态机 + 本地持久化"
      contains: "defineStore('defer'"
    - path: "src/features/chat/stores/taskStore.ts"
      provides: "task 状态机 + 本地持久化"
      contains: "defineStore('task'"
  key_links:
    - from: "src/features/chat/stores/deferStore.ts"
      to: "src/features/chat/types/defer.ts"
      via: "typed DeferItem/DeferStatus"
      pattern: "import type .* from '../types/defer'"
    - from: "src/features/chat/stores/taskStore.ts"
      to: "src/features/chat/types/task.ts"
      via: "typed TaskItem/TaskStatus/TaskSourceRef"
      pattern: "import type .* from '../types/task'"
---

<objective>
为 defer 与 message-to-task 闭环建立可测试、可持久化的类型与状态机基础，避免后续 UI 任务重复定义规则。

Purpose: 先固定 contracts + store 行为，后续组件只消费统一 action，防止状态漂移。
Output: defer/task 类型定义、Pinia store、对应单测基线。
</objective>

<execution_context>
@/Users/shikun/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/shikun/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-unified-inbox-triage/01-01-SUMMARY.md
@src/features/chat/stores/inboxStore.ts
@src/features/chat/stores/chatStore.ts

<interfaces>
From src/features/chat/stores/inboxStore.ts:
```ts
export const useInboxStore = defineStore('inbox', () => {
  const selectedItemIds = reactive(new Set<string>())
  function selectAll(itemIds: string[]) { /* ... */ }
  function markSelectedProcessed() { /* ... */ }
  return { selectedItemIds, selectAll, markSelectedProcessed }
})
```

From src/features/chat/stores/chatStore.ts:

```ts
const activeSidePanel = ref<'threads' | 'search' | 'pinned' | 'starred' | 'members' | 'settings' | null>(null)
function toggleSidePanel(panel: 'threads' | 'search' | 'pinned' | 'starred' | 'members' | 'settings')
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 定义 defer/task 接口合同并建立 Wave 0 测试骨架</name>
  <files>src/features/chat/types/defer.ts, src/features/chat/types/task.ts, tests/unit/stores/deferStore.test.ts, tests/unit/stores/taskStore.test.ts</files>
  <behavior>
    - Test 1: defer item 必含 roomId/eventId、dueAt、status，且默认 status=deferred。
    - Test 2: defer 从 deferred 迁移到 completed/archived 后不再属于 active 列表。
    - Test 3: task item 必含 assignee、dueAt、status、sourceRef(roomId+eventId)。
    - Test 4: task status 仅允许 todo -> doing -> done 及 doing -> todo 的受控迁移。
  </behavior>
  <action>创建 `defer.ts` 与 `task.ts` 类型文件，明确 storage key（versioned）、状态联合类型、source 引用结构；同时创建两份 store 单测文件，先用最小假实现/fixtures 固化行为输入输出。不要在组件里定义临时类型，所有后续实现必须 import 这两个合同文件。</action>
  <verify>
    <automated>pnpm vitest run tests/unit/stores/deferStore.test.ts tests/unit/stores/taskStore.test.ts</automated>
  </verify>
  <done>两份类型合同文件可被 import；两份测试文件存在并覆盖 defer/task 核心行为描述。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现 deferStore（提醒、排序字段、完成/归档流转）</name>
  <files>src/features/chat/stores/deferStore.ts, tests/unit/stores/deferStore.test.ts</files>
  <behavior>
    - Test 1: createDeferredItem 接收 preset/custom 时间并写入 dueAt。
    - Test 2: activeItems 始终只包含 status=deferred，且按 dueAt 升序。
    - Test 3: markCompleted/markArchived 后项移动到 historyItems，不再出现在 activeItems。
    - Test 4: hydrate 后可从 localStorage 恢复数据且 schema 无效时安全降级为空。
  </behavior>
  <action>按 Phase 1 模式实现 setup store：使用单一 source（Map 或数组）+ computed 派生 `activeItems/historyItems`，持久化采用 versioned key（如 `muon:defer:v1`），补齐 hydrate 容错。提醒时间计算集中在 store 内 helper（支持快捷预设 + 自定义时间），避免在多个组件重复实现。</action>
  <verify>
    <automated>pnpm vitest run tests/unit/stores/deferStore.test.ts</automated>
  </verify>
  <done>deferStore 提供创建、完成、归档、查询与持久化恢复 action；单测覆盖 INBX-04/05 的数据层行为。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 实现 taskStore（任务创建与 todo/doing/done 状态机）</name>
  <files>src/features/chat/stores/taskStore.ts, tests/unit/stores/taskStore.test.ts</files>
  <behavior>
    - Test 1: createTask 必须写入 assignee/dueAt/status/sourceRef。
    - Test 2: transitionStatus 只接受 todo/doing/done 合法状态，非法值抛错或拒绝。
    - Test 3: tasksByStatus 输出三组列表并保持同一任务仅存在于一个状态分组。
    - Test 4: hydrate 后任务可恢复，缺失关键字段的旧数据会被过滤。
  </behavior>
  <action>实现 `taskStore`，提供 `createTask/updateTask/transitionStatus` 与按状态分组的派生 getter；状态流转逻辑集中在 store action，不允许组件直接改 status 字段。持久化与迁移策略复用 Phase 1 风格（本地版本化存储 + 容错恢复）。</action>
  <verify>
    <automated>pnpm vitest run tests/unit/stores/taskStore.test.ts</automated>
  </verify>
  <done>taskStore 可稳定创建和流转任务，且状态机行为由自动化测试锁定（覆盖 TASK-03 与 TASK-01 数据字段约束）。</done>
</task>

</tasks>

<verification>
执行 `pnpm vitest run tests/unit/stores/deferStore.test.ts tests/unit/stores/taskStore.test.ts`，两组测试均通过。
</verification>

<success_criteria>

- Defer/Task 领域合同集中在 types 文件，无组件内重复定义。
- deferStore 与 taskStore 均具备可恢复持久化与受控状态迁移。
- 后续 UI 计划可直接消费 store action，无需再决定核心状态规则。
  </success_criteria>

<output>
After completion, create `.planning/phases/02-defer-and-message-to-task-loop/02-01-SUMMARY.md`
</output>
