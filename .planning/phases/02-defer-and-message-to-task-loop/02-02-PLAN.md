---
phase: 02-defer-and-message-to-task-loop
plan: "02"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/features/chat/components/UnifiedInboxPanel.vue
  - src/features/chat/components/MessageActionBar.vue
  - src/features/chat/components/DeferQueuePanel.vue
  - src/features/server/components/ChannelSidebar.vue
  - src/features/chat/stores/deferStore.ts
  - tests/components/DeferQueuePanel.test.ts
  - tests/components/MessageActionBar.test.ts
  - tests/components/UnifiedInboxPanel.test.ts
  - src/locales/en.json
  - src/locales/zh.json
autonomous: true
requirements: [INBX-04, INBX-05]
must_haves:
  truths:
    - "用户可从收件箱项直接设置 defer 提醒时间"
    - "用户可从消息 More 菜单执行 defer"
    - "defer 主队列按最早到期优先显示"
    - "defer 完成/归档项默认从主队列移除，并在历史视图可见"
  artifacts:
    - path: "src/features/chat/components/DeferQueuePanel.vue"
      provides: "defer active/history 视图与完成/归档动作"
      contains: "data-testid=defer-active-list"
    - path: "src/features/chat/components/UnifiedInboxPanel.vue"
      provides: "inbox 内 defer 入口"
      contains: "defer action trigger"
    - path: "src/features/chat/components/MessageActionBar.vue"
      provides: "消息 More 菜单 defer 入口"
      contains: "defer menu action"
  key_links:
    - from: "src/features/chat/components/UnifiedInboxPanel.vue"
      to: "src/features/chat/stores/deferStore.ts"
      via: "createDeferredItem action"
      pattern: "useDeferStore\(\).*createDeferredItem"
    - from: "src/features/chat/components/MessageActionBar.vue"
      to: "src/features/chat/stores/deferStore.ts"
      via: "menu defer action"
      pattern: "createDeferredItem\("
    - from: "src/features/server/components/ChannelSidebar.vue"
      to: "src/features/chat/components/DeferQueuePanel.vue"
      via: "DM sidebar render"
      pattern: "<DeferQueuePanel"
---

<objective>
交付 defer 的完整交互闭环：双入口创建提醒、主队列处理、历史视图沉淀。

Purpose: 满足用户“先延后、后处理”的真实工作流，避免待办被高流量消息淹没。
Output: DeferQueuePanel + 两个 defer 入口 + 完成/归档流转 UI。
</objective>

<execution_context>
@/Users/shikun/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/shikun/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-defer-and-message-to-task-loop/02-CONTEXT.md
@.planning/phases/02-defer-and-message-to-task-loop/02-RESEARCH.md
@src/features/chat/components/UnifiedInboxPanel.vue
@src/features/chat/components/MessageActionBar.vue
@src/features/server/components/ChannelSidebar.vue

<interfaces>
From src/features/chat/components/UnifiedInboxPanel.vue:
```ts
const emit = defineEmits<{ jump: [payload: { roomId: string, eventId: string }] }>()
```

From src/features/chat/components/MessageActionBar.vue:
```ts
const props = defineProps<{ event: any; roomId: string }>()
```

Locked decisions to honor:
- defer 入口必须同时出现在 inbox item 与 message More 菜单。
- defer 队列默认按 dueAt 升序。
- completed/archived 从主队列移除并进入历史视图。
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 在 UnifiedInboxPanel 提供 defer 快捷预设 + 自定义时间入口</name>
  <files>src/features/chat/components/UnifiedInboxPanel.vue, src/features/chat/stores/deferStore.ts, tests/components/UnifiedInboxPanel.test.ts, src/locales/en.json, src/locales/zh.json</files>
  <behavior>
    - Test 1: 对 inbox 项可选择快捷预设（1h/tonight/tomorrow morning/tomorrow）创建 defer。
    - Test 2: 可输入自定义时间并创建 defer。
    - Test 3: 创建后条目出现在 defer active 队列，且 dueAt 合法。
  </behavior>
  <action>在 `UnifiedInboxPanel` 的 item 操作区新增 defer 入口，调用 `deferStore.createDeferredItem`。必须实现“快捷预设 + 自定义时间”两种模式（按用户锁定决策），并把预设时间计算统一复用 store/helper，禁止组件内重复日期计算。补齐中英文文案 key。</action>
  <verify>
    <automated>pnpm vitest run tests/components/UnifiedInboxPanel.test.ts -t "defer"</automated>
  </verify>
  <done>用户可在 inbox 项内一键 defer，支持预设与自定义时间，测试覆盖入口与数据写入。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 在 MessageActionBar More 菜单加入 defer 入口并复用同一提醒逻辑</name>
  <files>src/features/chat/components/MessageActionBar.vue, src/features/chat/stores/deferStore.ts, tests/components/MessageActionBar.test.ts, src/locales/en.json, src/locales/zh.json</files>
  <behavior>
    - Test 1: More 菜单可触发 defer 并写入 source(roomId/eventId)。
    - Test 2: 与 inbox defer 使用同一 preset/custom 时间计算逻辑。
  </behavior>
  <action>在 More 菜单新增“Defer”动作，使用与 Task 1 相同的 defer 创建 API 与时间 helper。同步创建/更新 `tests/components/MessageActionBar.test.ts` 覆盖 defer 入口与写入断言，保证 verify 链路闭环。若研究建议有替代实现，仍按用户锁定决策保留双入口。不要新建平行 store 或本地状态副本。</action>
  <verify>
    <automated>pnpm vitest run tests/components/MessageActionBar.test.ts -t "defer"</automated>
  </verify>
  <done>消息 More 菜单可 defer，且写入数据结构与 inbox defer 完全一致。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 实现 DeferQueuePanel（Active/History）并接入 DM 侧边栏</name>
  <files>src/features/chat/components/DeferQueuePanel.vue, src/features/server/components/ChannelSidebar.vue, tests/components/DeferQueuePanel.test.ts, src/locales/en.json, src/locales/zh.json</files>
  <behavior>
    - Test 1: Active 列表按 dueAt 升序显示 deferred 项。
    - Test 2: completed/archived 操作后条目从 Active 移至 History。
    - Test 3: History 仅承载 completed/archived，不回流污染 Active。
  </behavior>
  <action>新增 `DeferQueuePanel`（建议同面板双 tab：Active/History，属于 Claude discretion），在 `ChannelSidebar` DM 模式挂载。Active 列表默认 dueAt 升序，完成/归档动作直接调用 store action。对逾期项增加清晰视觉提示，但不添加动画（保持执行效率导向）。</action>
  <verify>
    <automated>pnpm vitest run tests/components/DeferQueuePanel.test.ts</automated>
  </verify>
  <done>defer 队列可见且可操作：主队列干净、历史可追溯，满足 INBX-04/05。</done>
</task>

</tasks>

<verification>
运行：`pnpm vitest run tests/components/UnifiedInboxPanel.test.ts tests/components/MessageActionBar.test.ts tests/components/DeferQueuePanel.test.ts`。
</verification>

<success_criteria>
- defer 双入口（inbox + message More）均可用。
- Active 队列默认 dueAt 升序；completed/archived 不再出现在 Active。
- History 视图可查看已完成/归档项。
</success_criteria>

<output>
After completion, create `.planning/phases/02-defer-and-message-to-task-loop/02-02-SUMMARY.md`
</output>
