---
phase: 02-defer-and-message-to-task-loop
plan: "03"
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - src/features/chat/components/MessageActionBar.vue
  - src/features/chat/components/TaskComposerDialog.vue
  - src/features/chat/stores/taskStore.ts
  - tests/components/MessageActionBar.test.ts
  - src/locales/en.json
  - src/locales/zh.json
autonomous: true
requirements: [TASK-01]
must_haves:
  truths:
    - "用户可从任意消息创建任务，且任务包含 assignee/dueAt/status"
  artifacts:
    - path: "src/features/chat/components/TaskComposerDialog.vue"
      provides: "消息转任务表单（assignee/due/status）"
      contains: "task create form"
    - path: "src/features/chat/components/MessageActionBar.vue"
      provides: "消息 More 菜单转任务入口"
      contains: "convert to task action"
  key_links:
    - from: "src/features/chat/components/MessageActionBar.vue"
      to: "src/features/chat/stores/taskStore.ts"
      via: "createTask action"
      pattern: "createTask\("
---

<objective>
完成 message-to-task 的“创建链路”闭环：从消息触发到结构化任务落库。

Purpose: 先把 TASK-01 的输入合同与创建动作锁定，再在后续计划接入执行面板，降低单计划上下文压力。
Output: 任务创建入口 + TaskComposerDialog 提交链路。
</objective>

<execution_context>
@/Users/shikun/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/shikun/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/features/chat/components/MessageActionBar.vue
@src/features/chat/components/TaskComposerDialog.vue

<interfaces>
From src/features/chat/stores/taskStore.ts:
```ts
type TaskStatus = 'todo' | 'doing' | 'done'
type TaskSourceRef = { roomId: string; eventId: string }
function createTask(input: {
  title: string
  assignee: string
  dueAt: string
  status: TaskStatus
  sourceRef: TaskSourceRef
}): TaskItem
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 在消息 More 菜单接入“转任务”并完成任务创建表单</name>
  <files>src/features/chat/components/MessageActionBar.vue, src/features/chat/components/TaskComposerDialog.vue, src/features/chat/stores/taskStore.ts, tests/components/MessageActionBar.test.ts, src/locales/en.json, src/locales/zh.json</files>
  <behavior>
    - Test 1: 任意消息可打开 TaskComposerDialog。
    - Test 2: 提交时必须包含 assignee、dueAt、status（默认 todo）及 sourceRef(roomId/eventId)。
    - Test 3: 重复提交在 pending 锁期间不会产生重复任务。
  </behavior>
  <action>在 More 菜单加入“Convert to task”动作，弹出 `TaskComposerDialog` 收集 assignee/due/status。提交时调用 `taskStore.createTask`，sourceRef 强制使用当前消息 roomId/eventId（不可选填）。实现提交 pending 锁防止重复建任务。</action>
  <verify>
    <automated>pnpm vitest run tests/components/MessageActionBar.test.ts -t "create task from message"</automated>
  </verify>
  <done>从消息创建任务功能可用，且任务字段满足 TASK-01 的必填约束。</done>
</task>

</tasks>

<verification>
运行：`pnpm vitest run tests/components/MessageActionBar.test.ts -t "create task from message"`。
</verification>

<success_criteria>

- 任意消息可创建结构化任务（assignee/due/status/sourceRef）。
  </success_criteria>

<output>
After completion, create `.planning/phases/02-defer-and-message-to-task-loop/02-03-SUMMARY.md`
</output>
