---
phase: 02-defer-and-message-to-task-loop
plan: "04"
type: execute
wave: 4
depends_on: ["02-03"]
files_modified:
  - src/features/chat/components/TaskPanel.vue
  - src/features/chat/components/ChatWindow.vue
  - src/features/chat/stores/chatStore.ts
  - src/features/chat/stores/taskStore.ts
  - src/features/server/components/ChannelSidebar.vue
  - tests/components/TaskPanel.test.ts
  - src/locales/en.json
  - src/locales/zh.json
autonomous: true
requirements: [TASK-02, TASK-03]
must_haves:
  truths:
    - "用户可在 TaskPanel 中将任务在 todo/doing/done 三态间流转"
    - "用户可从 ChatWindow 侧边面板打开/关闭 TaskPanel"
    - "用户可从任务详情准确跳回对应源消息"
    - "任务回跳会复用现有 focusEventId 定位链路，而不是新增平行机制"
    - "上下文预加载失败时用户仍可完成跳转，不会被阻断"
  artifacts:
    - path: "src/features/chat/components/TaskPanel.vue"
      provides: "任务分栏 + 状态流转 + 回跳动作"
      contains: "transitionStatus"
    - path: "src/features/chat/components/ChatWindow.vue"
      provides: "task panel 渲染入口与开关"
      contains: "store.activeSidePanel === 'tasks'"
    - path: "src/features/server/components/ChannelSidebar.vue"
      provides: "任务面板入口与回跳链路并存"
      contains: "tasks panel trigger"
    - path: "tests/components/TaskPanel.test.ts"
      provides: "任务回跳成功/降级路径自动化覆盖"
      contains: "jump to source message"
  key_links:
    - from: "src/features/chat/components/ChatWindow.vue"
      to: "src/features/chat/components/TaskPanel.vue"
      via: "side panel mount/toggle"
      pattern: "activeSidePanel === 'tasks'"
    - from: "src/features/chat/components/TaskPanel.vue"
      to: "src/features/chat/stores/taskStore.ts"
      via: "transitionStatus action"
      pattern: "transitionStatus\("
    - from: "src/features/chat/components/TaskPanel.vue"
      to: "src/matrix/inbox.ts"
      via: "loadInboxEventContext before router push"
      pattern: "loadInboxEventContext\("
    - from: "src/features/chat/components/TaskPanel.vue"
      to: "src/features/chat/components/MessageList.vue"
      via: "router query focusEventId"
      pattern: "focusEventId"
---

<objective>
完成任务执行面板与回跳链路闭环：支持三态流转，并可一键回到源消息语境。

Purpose: 承接 02-03 的创建链路，把 TASK-03（执行流转）与 TASK-02（回跳语境）在同一执行面板内稳定交付。
Output: TaskPanel（流转 + 回跳）+ ChatWindow/Sidebar 接入 + 自动化覆盖。
</objective>

<execution_context>
@/Users/shikun/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/shikun/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-unified-inbox-triage/01-02-SUMMARY.md
@.planning/phases/01-unified-inbox-triage/01-03-SUMMARY.md
@src/matrix/inbox.ts
@src/features/chat/components/MessageList.vue
@src/features/chat/components/TaskPanel.vue

<interfaces>
From src/matrix/inbox.ts:
```ts
export interface InboxEventContext {
  roomId: string
  eventId: string
  eventsBefore: MatrixEvent[]
  event: MatrixEvent
  eventsAfter: MatrixEvent[]
}
export async function loadInboxEventContext(roomId: string, eventId: string, limit = 20): Promise<InboxEventContext>
```

From src/features/chat/components/MessageList.vue:
```ts
watch(() => route.query.focusEventId, async (value) => {
  pendingFocusEventId.value = typeof value === 'string' ? value : null
  if (pendingFocusEventId.value) await tryFocusEventFromQuery()
}, { immediate: true })
```

Locked decisions to honor:
- 任务回跳必须复用 Phase 1 已有“预加载上下文 + focusEventId”定位模式。
- 仅在 Phase 2 边界内交付任务回跳，不扩展检索/摘要能力。
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 实现 TaskPanel 三态流转并接入 ChatWindow 侧边面板</name>
  <files>src/features/chat/components/TaskPanel.vue, src/features/chat/components/ChatWindow.vue, src/features/chat/stores/chatStore.ts, src/features/chat/stores/taskStore.ts, tests/components/TaskPanel.test.ts, src/locales/en.json, src/locales/zh.json</files>
  <behavior>
    - Test 1: TaskPanel 展示 todo/doing/done 三组任务。
    - Test 2: 用户可把任务在三态间流转，且 UI 与 store 同步更新。
    - Test 3: 任务面板可从 ChatWindow 侧边面板入口打开/关闭。
  </behavior>
  <action>新增/完善 `TaskPanel`，按状态分组渲染任务；状态变更统一走 `taskStore.transitionStatus`。扩展 `chatStore.activeSidePanel` 增加 `'tasks'`（保持现有模式），并在 `ChatWindow` 挂载 TaskPanel 与开关入口；保持就地处理，不新建独立页面。</action>
  <verify>
    <automated>pnpm vitest run tests/components/TaskPanel.test.ts -t "transition task status"</automated>
  </verify>
  <done>任务面板可见且可操作，todo/doing/done 流转满足 TASK-03。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 在 TaskPanel 打通任务回跳源消息链路并补齐 Sidebar 协同入口</name>
  <files>src/features/chat/components/TaskPanel.vue, src/features/server/components/ChannelSidebar.vue, tests/components/TaskPanel.test.ts</files>
  <behavior>
    - Test 1: 点击任务“回到消息”会先调用 loadInboxEventContext(roomId,eventId)。
    - Test 2: 随后路由跳转到 `/dm/:roomId?focusEventId=:eventId`。
    - Test 3: context 预加载失败时仍降级导航（与 Phase 1 约定一致）。
  </behavior>
  <action>在 TaskPanel 中新增“Jump to source”动作，严格复用 Phase 1 跳转模式：先 `loadInboxEventContext`，再 `router.push` 带 `focusEventId`。失败路径仅告警并继续导航，不阻断用户。并在 `ChannelSidebar` 里为 DM 场景补齐任务面板入口协同，确保回跳链路可达。不要新增第二套消息定位机制。</action>
  <verify>
    <automated>pnpm vitest run tests/components/TaskPanel.test.ts -t "jump to source message"</automated>
  </verify>
  <done>任务详情可准确回到源消息并复用现有定位链路，且入口协同可达，满足 TASK-02。</done>
</task>

</tasks>

<verification>
运行：`pnpm vitest run tests/components/TaskPanel.test.ts -t "transition task status|jump to source message"`。
</verification>

<success_criteria>
- 任务面板支持 todo/doing/done 流转并保持状态一致。
- ChatWindow 侧边面板可稳定挂载/切换 TaskPanel。
- 任务卡片可一键回跳源消息并触发焦点定位。
- 预加载失败时仍可导航到目标会话，用户链路不断。
- 回跳链路复用既有机制（loadInboxEventContext + focusEventId），无平行实现。
</success_criteria>

<output>
After completion, create `.planning/phases/02-defer-and-message-to-task-loop/02-04-SUMMARY.md`
</output>
