---
phase: 02-defer-and-message-to-task-loop
plan: "05"
type: execute
wave: 5
depends_on: ["02-02", "02-04"]
files_modified:
  - src/features/server/components/ChannelSidebar.vue
  - src/features/chat/components/UnifiedInboxPanel.vue
  - src/features/chat/components/DeferQueuePanel.vue
  - tests/components/DeferQueuePanel.test.ts
autonomous: true
requirements: [INBX-04, INBX-05]
gap_closure: true
must_haves:
  truths:
    - "用户在 DM 侧栏可滚动访问 defer 队列全部事项"
    - "长 defer 列表下，用户仍可点击完成/归档处理非首屏事项"
    - "收件箱与 defer 组合布局不会因高度占位导致滚动失效"
  artifacts:
    - path: "src/features/server/components/ChannelSidebar.vue"
      provides: "DM 侧栏 inbox+defer 可滚动布局容器"
      contains: "shared scroll container for inbox/defer"
    - path: "src/features/chat/components/DeferQueuePanel.vue"
      provides: "Active/History 列表内部滚动与操作可达性"
      contains: "overflow-y-auto"
    - path: "src/features/chat/components/UnifiedInboxPanel.vue"
      provides: "侧栏场景安全高度策略"
      contains: "non-blocking height class"
    - path: "tests/components/DeferQueuePanel.test.ts"
      provides: "长列表滚动可达+可操作回归测试"
      contains: "scroll reachability"
  key_links:
    - from: "src/features/server/components/ChannelSidebar.vue"
      to: "src/features/chat/components/UnifiedInboxPanel.vue"
      via: "DM sidebar layout composition"
      pattern: "UnifiedInboxPanel"
    - from: "src/features/server/components/ChannelSidebar.vue"
      to: "src/features/chat/components/DeferQueuePanel.vue"
      via: "shared scrolling region"
      pattern: "DeferQueuePanel"
    - from: "tests/components/DeferQueuePanel.test.ts"
      to: "src/features/chat/components/DeferQueuePanel.vue"
      via: "long-list interaction assertions"
      pattern: "markCompleted|markArchived"
---

<objective>
修复 Phase 2 验证阻断：确保 defer 队列在真实 DM 侧栏中可滚动、可触达、可完成处理。

Purpose: 关闭 02-VERIFICATION 中“无法滚动”缺口，解除 INBX-04/INBX-05 的真实可用性阻断。
Output: 侧栏滚动布局修复 + DeferQueuePanel 内部滚动策略 + 长列表交互回归测试。
</objective>

<execution_context>
@/Users/shikun/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/shikun/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-defer-and-message-to-task-loop/02-VERIFICATION.md
@.planning/phases/02-defer-and-message-to-task-loop/02-02-PLAN.md
@.planning/phases/02-defer-and-message-to-task-loop/02-04-PLAN.md
@src/features/server/components/ChannelSidebar.vue
@src/features/chat/components/UnifiedInboxPanel.vue
@src/features/chat/components/DeferQueuePanel.vue

<interfaces>
Gap constraints from 02-VERIFICATION.md:
- 必须为 DM 侧栏 inbox+defer 区域建立可滚动布局；
- 必须为 DeferQueuePanel Active/History 提供内部滚动策略；
- 必须移除或调整 UnifiedInboxPanel 在侧栏场景的 `h-full` 占位；
- 必须补充“长列表仍可滚动并可点击完成/归档”的自动化测试。
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 修复 DM 侧栏与 defer 面板滚动布局，消除 h-full 挤压</name>
  <files>src/features/server/components/ChannelSidebar.vue, src/features/chat/components/UnifiedInboxPanel.vue, src/features/chat/components/DeferQueuePanel.vue</files>
  <behavior>
    - Test 1: DM 侧栏中 inbox+defer 区域在内容增长时存在可用滚动容器。
    - Test 2: DeferQueuePanel 的 Active/History 在长列表下具备内部滚动（max-height + overflow-y-auto）。
    - Test 3: UnifiedInboxPanel 在侧栏组合场景不再使用会抢占高度的 `h-full` 策略。
  </behavior>
  <action>按验证缺口只做滚动修复：在 `ChannelSidebar.vue` 为 DM 模式下 `UnifiedInboxPanel` 与 `DeferQueuePanel` 建立共享可滚动布局；在 `DeferQueuePanel.vue` 为 Active/History 列表增加高度约束与 `overflow-y-auto`，保证列表增长时操作按钮仍可达；在 `UnifiedInboxPanel.vue` 调整根节点高度类，移除/替换侧栏场景的 `h-full` 占位。禁止引入新功能、禁止改动 defer 状态机语义。</action>
  <verify>
    <automated>pnpm vitest run tests/components/DeferQueuePanel.test.ts -t "defer queue"</automated>
  </verify>
  <done>真实侧栏布局具备连续滚动链路，长队列不再截断，非首屏事项可通过滚动触达。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 补齐长列表滚动可达与完成/归档可操作性回归测试</name>
  <files>tests/components/DeferQueuePanel.test.ts</files>
  <behavior>
    - Test 1: 构造长 defer 列表时，组件渲染出的列表容器带有可滚动样式/类。
    - Test 2: 在长列表场景可对非首屏项触发 completed/archived 动作。
    - Test 3: 状态迁移后该项从 Active 移除并可在 History 视图断言。
  </behavior>
  <action>扩展 `DeferQueuePanel` 组件测试，新增“长列表可滚动且可处理”用例：构造超出可视高度的 Active 数据，断言滚动容器存在；对靠后条目执行完成/归档动作并验证 store/action 结果与视图迁移。测试只覆盖本次缺口，不添加无关断言。</action>
  <verify>
    <automated>pnpm vitest run tests/components/DeferQueuePanel.test.ts -t "long list"</automated>
  </verify>
  <done>滚动可达性阻断场景被自动化覆盖，后续回归可在 CI 中直接发现。</done>
</task>

</tasks>

<verification>
运行：`pnpm vitest run tests/components/DeferQueuePanel.test.ts`。
</verification>

<success_criteria>

- DM 侧栏下 defer 区域可滚动访问全部事项。
- 长列表情况下可完成/归档非首屏事项，不被布局阻断。
- INBX-04 与 INBX-05 的阻断缺口在自动化测试中可复现并被修复。
  </success_criteria>

<output>
After completion, create `.planning/phases/02-defer-and-message-to-task-loop/02-05-SUMMARY.md`
</output>
