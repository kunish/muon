/**
 * Quick-reaction emoji shown in the one-tap row of both reaction entry points
 * (the in-bubble ReactionBar "+" picker and the hover-bar ReactionPickerPopover).
 * Single source of truth so both surfaces offer the same set.
 */
export const QUICK_REACTION_EMOJIS = ['👍', '❤️', '😂', '😮', '😢', '🎉', '🔥', '👏'] as const
