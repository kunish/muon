{
  "name": "@muon/api",
  "type": "module",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "tsc --noEmit --project tsconfig.json",
    "dev": "node --env-file=../../docker/.env --import tsx src/server.ts",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "catalog:",
    "@muon/enterprise-contracts": "workspace:*",
    "effect": "catalog:",
    "pg": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@types/pg": "catalog:",
    "tsx": "catalog:",
    "vitest": "catalog:"
  }
}
